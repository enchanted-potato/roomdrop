---
phase: quick-260704-hv0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [justfile]
autonomous: true
requirements: [QUICK-justfile]

must_haves:
  truths:
    - "Running `just --list` shows all recipes with descriptive comments"
    - "Each recipe wraps the matching pnpm script (or pnpm install)"
    - "`just install`, `just dev`, `just build`, `just test`, etc. run the correct pnpm command"
  artifacts:
    - path: "justfile"
      provides: "Task runner recipes for dev, build, preview, test, typecheck, lint, format, format:check, install"
      contains: "dev:"
  key_links:
    - from: "justfile"
      to: "package.json scripts"
      via: "pnpm run <script> / pnpm install"
      pattern: "pnpm"
---

<objective>
Create a `justfile` at the repo root that wraps the project's pnpm commands, each with a
`just`-visible comment describing what it does.

Purpose: Give a single discoverable entrypoint (`just --list`) for the common dev commands.
Output: `justfile` at repo root with recipes: install, dev, build, preview, test, typecheck,
lint, format, format-check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Source of truth for the scripts being wrapped:
@package.json
</context>

<interfaces>
<!-- package.json scripts (pnpm@11.9.0 is the declared packageManager) -->
- dev          -> vite
- build        -> vite build
- preview      -> vite preview
- test         -> vitest run --passWithNoTests
- typecheck    -> tsc --noEmit
- lint         -> eslint .
- format       -> prettier --write .
- format:check -> prettier --check .
<!-- install has no script; use `pnpm install` directly -->
<!-- just recipe names cannot contain a colon; use `format-check` for the format:check script -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write the justfile</name>
  <files>justfile</files>
  <action>
    Create `justfile` at the repo root. Add a `default` recipe that runs `just --list` so
    bare `just` shows the menu. Then add one recipe per command below, each preceded by a
    `#` comment (just renders these comments in `just --list`). All recipes invoke pnpm:

    - install: `pnpm install` — comment: install dependencies with pnpm
    - dev: `pnpm run dev` — comment: start the Vite dev server
    - build: `pnpm run build` — comment: build the production bundle
    - preview: `pnpm run preview` — comment: serve the production build locally
    - test: `pnpm run test` — comment: run the Vitest test suite (passes with no tests)
    - typecheck: `pnpm run typecheck` — comment: type-check with tsc (no emit)
    - lint: `pnpm run lint` — comment: lint the codebase with ESLint
    - format: `pnpm run format` — comment: format all files with Prettier
    - format-check: `pnpm run format:check` — comment: check formatting without writing

    Note the recipe wrapping `format:check` must be named `format-check` because just recipe
    names cannot contain a colon; the recipe body still calls `pnpm run format:check`.
    Do not add recipes for anything not listed above.
  </action>
  <verify>
    <automated>cd /Users/kristiakarakatsani/Repos/roomdrop && just --list && just --summary | tr ' ' '\n' | grep -qx dev && just --summary | tr ' ' '\n' | grep -qx format-check && just --summary | tr ' ' '\n' | grep -qx install</automated>
  </verify>
  <done>`just --list` runs without error and displays install, dev, build, preview, test, typecheck, lint, format, and format-check, each with a description comment; every recipe body invokes pnpm.</done>
</task>

</tasks>

<verification>
- `just --list` succeeds and shows all nine recipes with comments.
- Each recipe wraps the corresponding pnpm script (or `pnpm install`).
</verification>

<success_criteria>
- `justfile` exists at repo root.
- All commands from package.json are represented, plus an install recipe.
- Each recipe has a comment that appears in `just --list`.
- pnpm is used for every recipe.
</success_criteria>

<output>
Create `.planning/quick/260704-hv0-create-a-justfile-at-the-repo-root-with-/260704-hv0-SUMMARY.md` when done
</output>
