# Phase 6: Deploy & PWA - Context

**Gathered:** 2026-07-04
**Status:** Ready for planning
**Mode:** Auto-accepted (autonomous run, config mode: yolo)
**Scope note:** Per user instruction, this phase is prepared LOCALLY only — PWA build + docs; no actual DanubeData deploy is executed. Deploy steps land in PRODUCTIONISE.md.

<domain>
## Phase Boundary
PWA with model runtime caching, build-ID footer, and update toast; deploy runbook documented. Covers BGR-07 and the local half of the Phase 6 success criteria.
</domain>

<decisions>
## Implementation Decisions
- `vite-plugin-pwa` with `registerType: 'prompt'` → "New version available" toast with a Refresh action (Pitfall M14/m4 — no silent stale shell).
- Workbox runtime caching: `CacheFirst` scoped to `https://staticimgly.com/*` only (model + WASM artifacts, 1-year expiry) — never precache the model, never cache-everything (Pitfall C7).
- `?nosw=1` emergency bypass: skips registration and unregisters existing SWs (Pitfall m4).
- Build ID: git short SHA injected via Vite `define` (`__BUILD_ID__`), shown in the footer next to "Open source".
- Icons generated by `scripts/generate-icons.mjs` (sharp; accent square + "R" mark): 192/512/maskable/apple-touch, committed to `public/`.
- Manifest: standalone display, theme/background `#f1ebe1`.
</decisions>

<code_context>
## Existing Code Insights
- Footer has an empty build-ID slot reserved since Phase 1's UI-SPEC.
- Toast store's action button (Phase 2) carries the Refresh CTA.
</code_context>

<specifics>
## Specific Ideas
None.
</specifics>

<deferred>
## Deferred Ideas
- Actual DanubeData deploy + bandwidth monitoring — PRODUCTIONISE.md runbook.
</deferred>
