# Plan 02-01: Library & Editor (single plan)

**Phase:** 2 — Library & Editor (originals)
**Status:** planned
**Requirements:** UPL-02, UPL-04, UPL-05, EDT-01..11, PER-01

## Tasks

1. **Schema + store actions** — flesh out `Placement` (room-space coords), add `inLibrary` to `LibraryItem`; actions: add/update/remove/duplicate placement, reorder z, remove library item with refcounted blob GC; one-level delete undo buffer (non-persisted). Unit tests for store logic.
2. **Toast action support** — `toastStore` gains optional `action {label, onAction}` + `info` variant; `Toast`/`ToastHost` render it.
3. **Shared hooks** — `useBlobUrl(blobId)` (object URL lifecycle), `useHtmlImage(url)` (HTMLImageElement loader for Konva).
4. **Library feature** — `useLibraryUpload` (multi-file through ImagePipeline → `lib:` blobs → store), `LibraryPanel` (strip/rail, add tile, thumbs with badge + delete), tap-to-place + desktop drag source.
5. **Editor feature** — `EditorStage` (Konva Stage, ResizeObserver sizing, room layer, placements layer, Transformer layer, selection state, two-finger pinch/rotate, drop target), `PlacedItem`, `SelectionToolbar` (forward/backward/duplicate/flip/delete+undo).
6. **App wiring** — room-present view becomes editor stage + library panel; self-heal (D-09) logic preserved in stage room loading.

## Verification
- `pnpm typecheck && pnpm lint && pnpm test && pnpm build` green.
- Success criteria 1–5 of Phase 2 traced in SUMMARY.
