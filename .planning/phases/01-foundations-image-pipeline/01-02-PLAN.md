---
phase: 01-foundations-image-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - index.html
  - src/styles/index.css
  - src/app/App.tsx
  - src/app/AppShell.tsx
  - src/app/Header.tsx
  - src/components/Footer.tsx
  - src/components/Toast.tsx
  - src/components/ToastHost.tsx
  - src/store/toastStore.ts
  - src/features/room/RoomDropzone.tsx
  - src/features/room/SkeletonRoom.tsx
autonomous: false
requirements:
  - FND-03
  - UPL-01
tags:
  - ui-shell
  - tailwind
  - mobile-first
  - dropzone

must_haves:
  truths:
    - "App renders correctly on iPhone SE (375×667) viewport with no horizontal scroll (FND-03)"
    - "AppShell uses `min-height: 100svh` and `env(safe-area-inset-*)` padding (FND-03)"
    - "Marcellus + Mulish fonts load via Google Fonts <link> with display=swap and a preconnect (UI-SPEC)"
    - "Empty-state RoomDropzone renders the locked headline 'Upload a photo of your room', body copy, and a 'Choose photo' CTA (UI-SPEC)"
    - "RoomDropzone's hidden <input> has `type=file` + `accept=image/*` + `capture=environment` so iOS opens the camera/photo picker (UPL-01)"
    - "RoomDropzone supports HTML5 drag-and-drop on desktop (dragover tints border to --accent, drop calls the same handler as picker selection)"
    - "Toast component + toastStore exist; can be shown/dismissed; auto-dismiss after 8s; manual dismiss button present"
    - "SkeletonRoom renders a 16:9 --surface rectangle with a 1.5s opacity pulse and `aria-label='Loading your room photo'`"
    - "Header conditionally renders 'Change room photo' only when a roomId prop is supplied (Plan 04 wires the real store)"
    - "Tailwind v4 @theme block declares all UI-SPEC tokens (--bg, --surface, --accent, --danger, --ink, --ink-mut, --ink-fnt, --border, --border-d)"
    - "Touch targets for 'Choose photo' and 'Change room photo' are ≥44px high (Apple HIG / UI-SPEC)"
    - "Body and label text are ≥16px so iOS does not auto-zoom on focus"
  artifacts:
    - path: "src/app/AppShell.tsx"
      provides: "Top-level layout with safe-area padding and 100svh height"
      exports: ["AppShell"]
    - path: "src/app/Header.tsx"
      provides: "Header with wordmark, tagline (desktop), conditional Change room photo CTA"
      exports: ["Header"]
    - path: "src/features/room/RoomDropzone.tsx"
      provides: "Empty-state dropzone with hidden file input and drag/drop handlers"
      exports: ["RoomDropzone"]
    - path: "src/features/room/SkeletonRoom.tsx"
      provides: "Hydration skeleton placeholder"
      exports: ["SkeletonRoom"]
    - path: "src/components/Toast.tsx"
      provides: "Single-slot error toast with manual + auto dismiss"
      exports: ["Toast"]
    - path: "src/store/toastStore.ts"
      provides: "Imperative toast queue (separate from useAppStore to avoid persisting transient UI)"
      exports: ["showToast", "useToast"]
    - path: "src/styles/index.css"
      provides: "Tailwind v4 entry + @theme tokens + safe-area + svh utilities"
      contains: "@theme"
  key_links:
    - from: "src/app/App.tsx"
      to: "src/app/AppShell.tsx"
      via: "renders <AppShell><Header/><main>...<Footer/></AppShell>"
    - from: "src/features/room/RoomDropzone.tsx"
      to: "props.onFile callback"
      via: "called on picker selection AND on drop with the selected File"
    - from: "src/components/Toast.tsx"
      to: "src/store/toastStore.ts"
      via: "useToast() hook"
---

<objective>
Build the visual + interaction layer of Phase 1's app shell — without yet wiring real image processing or persistence. After this plan, the app *looks like* RoomDrop and *feels like* RoomDrop on a phone: safe-area-aware, properly typed, with the locked UI-SPEC copy, dropzone interactions, and a toast slot ready to surface errors when Plan 04 wires the real pipeline.

This plan runs in Wave 2 in parallel with Plan 03 (pure pipeline/store logic). The two plans are designed with **zero file overlap** — Plan 02 owns `src/app/*`, `src/components/*`, `src/features/room/RoomDropzone.tsx`, `src/features/room/SkeletonRoom.tsx`, `src/store/toastStore.ts`, and `src/styles/index.css`; Plan 03 owns `src/lib/image-pipeline/*`, `src/lib/idb/blobIds.ts`, `src/store/useAppStore.ts`, and Vitest test fixtures.

Purpose: lock UI-SPEC copy, tokens, and interactions in code so Plan 04 only needs to call `onFile(file)` and `showToast()` — no UI rework.
Output: a deploy-ready visual shell that passes mobile-shell smoke testing on iPhone SE viewport.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundations-image-pipeline/01-CONTEXT.md
@.planning/phases/01-foundations-image-pipeline/01-RESEARCH.md
@.planning/phases/01-foundations-image-pipeline/01-UI-SPEC.md
@.planning/phases/01-foundations-image-pipeline/SKELETON.md
@.planning/phases/01-foundations-image-pipeline/01-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Contracts this plan defines, to be consumed by Plan 04. -->

From src/features/room/RoomDropzone.tsx:
```typescript
export interface RoomDropzoneProps {
  onFile: (file: File) => void;            // Plan 04 will pass the pipeline-running handler
}
export function RoomDropzone(props: RoomDropzoneProps): JSX.Element;
```

From src/store/toastStore.ts:
```typescript
export type ToastVariant = 'error';        // Phase 1 only emits error toasts (UI-SPEC)
export interface ToastSpec { title: string; body: string; variant: ToastVariant; }
export function showToast(spec: ToastSpec): void;        // imperative API
export function useToast(): ToastSpec | null;            // hook for ToastHost
export function dismissToast(): void;
```

From src/app/Header.tsx:
```typescript
export interface HeaderProps {
  hasActiveRoom: boolean;                  // Plan 04 will derive from useAppStore
  onChangeRoom?: () => void;               // Plan 04 will pass picker-trigger
}
```

<!-- Plan 01-01 already created: src/app/config.ts (REPO_URL), src/components/Footer.tsx, src/lib/idb/index.ts. -->
<!-- Plan 02 will refactor App.tsx to use AppShell but keep Plan 01's skeletonPing call until Plan 04 replaces it with the real hydration. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add UI-SPEC design tokens, Google Fonts, safe-area utilities to Tailwind v4 entry CSS and index.html</name>
  <files>index.html, src/styles/index.css</files>
  <read_first>
    - index.html (Plan 01 baseline)
    - src/styles/index.css (Plan 01 baseline — just `@import "tailwindcss";`)
    - .planning/phases/01-foundations-image-pipeline/01-UI-SPEC.md §"Design System", §"Color", §"Typography", §"Spacing Scale"
    - .planning/phases/01-foundations-image-pipeline/01-RESEARCH.md §"iPhone SE → desktop responsive shell" (viewport meta + 100svh + safe-area)
    - .planning/phases/01-foundations-image-pipeline/01-CONTEXT.md (D-11 src layout — styles live under src/styles)
  </read_first>
  <behavior>
    - Test 1: `index.html` `<head>` contains the Marcellus + Mulish Google Fonts `<link>` with `display=swap` and a `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>`
    - Test 2: `src/styles/index.css` contains a `@theme` block declaring every UI-SPEC color token (`--bg`, `--surface`, `--accent`, `--danger`, `--ink`, `--ink-mut`, `--ink-fnt`, `--border`, `--border-d`) with the exact hex values from UI-SPEC §"Color"
    - Test 3: CSS declares font families `--font-display: "Marcellus", serif;` and `--font-body: "Mulish", system-ui, sans-serif;`
    - Test 4: `body` rule sets `min-height: 100svh` and `background: var(--bg); color: var(--ink); font-family: var(--font-body);`
    - Test 5: An `.app-shell` utility (or `@layer base` rule) applies `min-height: 100svh` + `padding-top: env(safe-area-inset-top)` + bottom/left/right safe-area padding
    - Test 6: `pnpm build` succeeds; Tailwind utilities + custom tokens both compile (assert by greppping for one declared token in `dist/assets/index-*.css`)
  </behavior>
  <action>
    Update `index.html` `<head>` to add (in order, before the existing `<script type="module">`): `<link rel="preconnect" href="https://fonts.googleapis.com">`, `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>`, and `<link href="https://fonts.googleapis.com/css2?family=Marcellus&family=Mulish:wght@400;700&display=swap" rel="stylesheet">` (per UI-SPEC §"Design System" font loading rule). Do NOT change the viewport meta — Plan 01 already locked the correct value.

    Update `src/styles/index.css`:
    1. Keep `@import "tailwindcss";` at the top.
    2. Add a Tailwind v4 `@theme` block declaring `--color-bg`, `--color-surface`, `--color-accent`, `--color-danger`, `--color-ink`, `--color-ink-mut`, `--color-ink-fnt`, `--color-border`, `--color-border-d` (Tailwind v4 prefixes its colors with `--color-`; this exposes them as `bg-bg`, `bg-surface`, etc. utilities). Include `--font-display: "Marcellus", serif;` and `--font-body: "Mulish", system-ui, sans-serif;`. Use the exact hex values from UI-SPEC §"Color".
    3. Mirror the tokens as raw CSS custom properties on `:root` (`--bg: #f1ebe1; ... ;`) so legacy `var(--bg)` usage works in inline styles when needed.
    4. Add a `body { min-height: 100svh; background: var(--bg); color: var(--ink); font-family: var(--font-body); }` rule.
    5. Add a `.app-shell { min-height: 100svh; padding-top: env(safe-area-inset-top); padding-bottom: env(safe-area-inset-bottom); padding-left: env(safe-area-inset-left); padding-right: env(safe-area-inset-right); display: flex; flex-direction: column; }` rule. Children layout (Header / main / Footer) will be controlled by AppShell.
    6. Add a `@keyframes skeleton-pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.55; } }` rule for the SkeletonRoom (1.5s duration; applied via class in Task 3).
    7. Add a `:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px; }` rule per UI-SPEC §"Color" focus ring.

    Do NOT introduce `tailwind.config.js` (Tailwind v4 forbids it for `@theme` configs). Do NOT self-host fonts (Phase 6 will consider it).
  </action>
  <verify>
    <automated>pnpm build && grep -q "Marcellus" index.html && grep -q "Mulish" index.html && grep -q "preconnect" index.html && grep -q "@theme" src/styles/index.css && grep -q "100svh" src/styles/index.css && grep -q "safe-area-inset-top" src/styles/index.css && grep -q "f1ebe1" src/styles/index.css</automated>
  </verify>
  <done>
    `index.html` loads Marcellus + Mulish with preconnect; `src/styles/index.css` declares the full UI-SPEC token palette via `@theme` + `:root`, sets `100svh` body height, declares safe-area utility, and `pnpm build` succeeds with the tokens preserved in the output CSS.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build AppShell + Header + refactor App.tsx + update Footer to use design tokens; refactor App.tsx to render the new shell</name>
  <files>src/app/AppShell.tsx, src/app/Header.tsx, src/app/App.tsx, src/components/Footer.tsx</files>
  <read_first>
    - src/app/App.tsx (Plan 01 baseline — `<h1>RoomDrop</h1>` + skeletonPing + Choose photo button + Footer; will be refactored to host AppShell)
    - src/components/Footer.tsx (Plan 01 baseline — already has AGPL copy + REPO_URL link)
    - .planning/phases/01-foundations-image-pipeline/01-UI-SPEC.md §"Component Inventory", §"Copywriting Contract" (wordmark "RoomDrop", tagline "Preview a piece before you buy it", "Change room photo" copy)
    - .planning/phases/01-foundations-image-pipeline/01-CONTEXT.md (D-11 src/app layout; tagline desktop-only per UI-SPEC)
    - .planning/phases/01-foundations-image-pipeline/01-RESEARCH.md §"Component Responsibilities" (AppShell/Header/Footer ownership)
  </read_first>
  <behavior>
    - Test 1: `<AppShell>` renders a `<div class="app-shell">` wrapping `props.children`; the class is the one defined in Task 1's CSS
    - Test 2: `<Header>` always renders the wordmark `RoomDrop` (Marcellus, 28px); always renders the tagline `Preview a piece before you buy it` but hides it on small viewports via Tailwind responsive utilities (e.g. `hidden md:block`)
    - Test 3: `<Header hasActiveRoom={false}>` does NOT render a "Change room photo" button
    - Test 4: `<Header hasActiveRoom={true} onChangeRoom={fn}>` renders a button with text exactly `Change room photo` (sentence case per UI-SPEC); clicking calls `onChangeRoom`
    - Test 5: The "Change room photo" button has a min height of 44px (Apple HIG)
    - Test 6: Footer still renders the locked AGPL copy + REPO_URL link unchanged from Plan 01, but switches inline styles to use the `text-ink-mut bg-surface` token utilities and applies muted-ink color
    - Test 7: `App.tsx` now renders `<AppShell><Header hasActiveRoom={false} /><main className="flex-1">…skeleton ping debug + Choose photo button stub…</main><Footer /></AppShell>`. The Plan 01 skeletonPing display + Choose photo button remain (Plan 04 will replace `<main>` content with `<RoomDropzone>` / `<RoomCanvas>`).
  </behavior>
  <action>
    Write `src/app/AppShell.tsx`: a functional component accepting `children: ReactNode`. Render `<div className="app-shell">{children}</div>`. The `app-shell` class was defined in Task 1's CSS; AppShell does NOT need to re-declare safe-area padding inline.

    Write `src/app/Header.tsx`: a functional component with props `{ hasActiveRoom: boolean; onChangeRoom?: () => void }`. Render `<header className="bg-surface border-b border-border px-4 md:px-8 py-3 flex items-center justify-between">` containing on the left a `<div>` with the wordmark (`<span style={{ fontFamily: 'var(--font-display)', fontSize: '28px', lineHeight: 1.1 }}>RoomDrop</span>`) and the tagline (`<span className="hidden md:inline ml-3 text-ink-mut" style={{ fontSize: '14px' }}>Preview a piece before you buy it</span>`). On the right, conditionally render `{hasActiveRoom && <button type="button" onClick={onChangeRoom} className="min-h-[44px] px-4 text-ink font-bold" style={{ fontSize: '14px' }}>Change room photo</button>}`. Per UI-SPEC §"Interaction Contract", Phase 1 does NOT show a destructive-confirm dialog when this button is clicked (Phase 3 adds that).

    Update `src/components/Footer.tsx`: keep the AGPL copy + `REPO_URL` link exactly, but switch the wrapping `<footer>` element to `className="bg-surface border-t border-border text-ink-mut text-center py-4 px-4"` using the tokens from Task 1. Keep `target="_blank" rel="noopener"`. Font-size 16 per UI-SPEC body role.

    Update `src/app/App.tsx`: refactor to render `<AppShell><Header hasActiveRoom={false} /><main className="flex-1 flex items-center justify-center px-4"><div className="text-center">{/* keep Plan 01's skeleton ping display + Choose photo button stub here — Plan 04 will replace the <main> body with <RoomDropzone> / <RoomCanvas> */}</div></main><Footer /></AppShell>`. Keep the `useEffect` skeletonPing call so the IDB round-trip continues to work end-to-end through Plan 02's intermediate state.

    Do NOT add real `RoomDropzone` mounting in App.tsx yet — that wiring is Plan 04's job. Do NOT touch `src/lib/idb/index.ts` or `src/app/config.ts`.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm build && grep -q "Preview a piece before you buy it" src/app/Header.tsx && grep -q "Change room photo" src/app/Header.tsx && grep -q "app-shell" src/app/AppShell.tsx && grep -q "AppShell" src/app/App.tsx && grep -q "Header" src/app/App.tsx</automated>
  </verify>
  <done>
    AppShell, Header, and Footer use UI-SPEC tokens + locked copy; App.tsx hosts them around a placeholder main containing the Plan 01 skeleton-ping demo; typecheck + build pass; the conditional "Change room photo" button is present in Header source and gated on `hasActiveRoom`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build RoomDropzone (empty-state, picker + drag/drop) + SkeletonRoom + Toast + toastStore + ToastHost</name>
  <files>src/features/room/RoomDropzone.tsx, src/features/room/SkeletonRoom.tsx, src/components/Toast.tsx, src/components/ToastHost.tsx, src/store/toastStore.ts</files>
  <read_first>
    - src/styles/index.css (Task 1 of this plan — for `skeleton-pulse` keyframes + token names)
    - src/app/App.tsx (Task 2 of this plan — to understand where ToastHost will mount in Plan 04)
    - .planning/phases/01-foundations-image-pipeline/01-UI-SPEC.md §"Component Inventory", §"Interaction Contract", §"Copywriting Contract" (RoomDropzone copy: "Upload a photo of your room" + body + secondary line; Toast copy: locked HEIC + generic upload errors; SkeletonRoom aria-label)
    - .planning/phases/01-foundations-image-pipeline/01-CONTEXT.md (D-11 features/room layout; deferred items list — no destructive confirm, no library yet)
    - .planning/phases/01-foundations-image-pipeline/01-RESEARCH.md §"Mobile Shell & Safe-Area Reference" (touch target 44px, body font ≥16px, `capture=environment` for camera)
  </read_first>
  <behavior>
    - Test 1: `<RoomDropzone onFile={fn}>` renders a clickable region with the locked empty-state copy (headline `Upload a photo of your room`, body `A clear shot in good light works best. Your photo stays on this device — nothing is uploaded.`, secondary `Tap to open camera or photo library.`, primary CTA `Choose photo`)
    - Test 2: RoomDropzone contains a hidden `<input type="file" accept="image/*" capture="environment">` reachable via ref
    - Test 3: Clicking the dropzone OR the CTA button calls `.click()` on the hidden input
    - Test 4: When the input's change event fires with a `File`, `onFile(file)` is called with that File
    - Test 5: `dragover` on the dropzone calls `event.preventDefault()` and toggles a class adding `border-accent` styling; `dragleave` removes it; `drop` calls `event.preventDefault()`, reads `event.dataTransfer.files[0]`, and calls `onFile(file)` if it exists
    - Test 6: `<SkeletonRoom>` renders a 16:9-aspect div with `bg-surface` styling, `aria-label="Loading your room photo"`, and the `animation: skeleton-pulse 1.5s ease-in-out infinite` style
    - Test 7: `toastStore.showToast({ title, body, variant: 'error' })` updates internal state; `useToast()` returns that ToastSpec; `dismissToast()` clears it; after 8 seconds an internal `setTimeout` auto-clears
    - Test 8: `<Toast>` renders the title (label role: 14px bold), body (body role: 16px), a dismiss `<button aria-label="Dismiss">`, with the `--danger` left accent bar treatment per UI-SPEC
    - Test 9: `<ToastHost>` reads `useToast()` and renders `<Toast>` only when a toast is active; passes the dismiss handler down
    - Test 10: Toast title and body are configurable — verify by calling `showToast({ title: 'That photo format isn't supported yet', body: 'iPhone HEIC photos can't be opened…', variant: 'error' })` from a test
  </behavior>
  <action>
    Write `src/features/room/RoomDropzone.tsx`: a functional component with `RoomDropzoneProps = { onFile: (file: File) => void }`. Use `useRef<HTMLInputElement>(null)` for the hidden input. Use `useState<boolean>(false)` for the dragover visual state. Render `<section role="button" tabIndex={0} onClick={() => inputRef.current?.click()} onDragOver={...} onDragLeave={...} onDrop={...} className={"border-2 border-dashed rounded-2xl bg-surface p-6 md:p-12 text-center cursor-pointer transition-colors " + (isDragover ? "border-accent bg-accent/5" : "border-border-d")}>` containing: an icon from `lucide-react` (`<ImagePlus />` or `<Camera />`, ~32px), the headline `<h2 style={{ fontFamily: 'var(--font-display)', fontSize: 'clamp(20px, 5vw, 22px)' }}>Upload a photo of your room</h2>`, body `<p className="text-ink-mut mt-2" style={{ fontSize: '16px', lineHeight: 1.5 }}>A clear shot in good light works best. Your photo stays on this device — nothing is uploaded.</p>`, secondary `<p className="text-ink-fnt mt-1" style={{ fontSize: '14px' }}>Tap to open camera or photo library.</p>`, and a primary CTA `<button type="button" className="mt-4 min-h-[44px] px-6 bg-accent text-white font-bold rounded-md" style={{ fontSize: '14px' }} onClick={(e) => { e.stopPropagation(); inputRef.current?.click(); }}>Choose photo</button>`. The hidden `<input ref={inputRef} type="file" accept="image/*" capture="environment" style={{ display: 'none' }} onChange={(e) => { const file = e.target.files?.[0]; if (file) { onFile(file); e.target.value = ''; } }}>`. The reset `e.target.value = ''` is required so picking the same file twice re-fires `onChange` (a known browser quirk).

    Wire `onDragOver={(e) => { e.preventDefault(); setIsDragover(true); }}`, `onDragLeave={() => setIsDragover(false)}`, `onDrop={(e) => { e.preventDefault(); setIsDragover(false); const file = e.dataTransfer.files?.[0]; if (file) onFile(file); }}`. Per CONTEXT D-08, "Change room photo" cross-fade is Plan 04's concern; this component only emits `onFile`.

    Write `src/features/room/SkeletonRoom.tsx`: a functional component (no props other than `aria-label` defaulting to `"Loading your room photo"`). Render `<div role="status" aria-label={ariaLabel} className="bg-surface rounded-2xl w-full" style={{ aspectRatio: '16 / 9', animation: 'skeleton-pulse 1.5s ease-in-out infinite' }} />`. Tailwind v4 supports `aspect-video` utility; either is acceptable.

    Write `src/store/toastStore.ts`: a minimal Zustand-free pub/sub (use plain `useSyncExternalStore` or a Zustand vanilla store — your call, but DO NOT use the persisted `useAppStore` because toasts are transient UI and would bloat the persisted payload). Export `ToastVariant = 'error'`, `ToastSpec = { title: string; body: string; variant: ToastVariant }`, `showToast(spec: ToastSpec): void` (replaces any existing toast, FIFO single-slot per UI-SPEC; starts an 8000ms `setTimeout` to auto-dismiss; clears any prior timer), `dismissToast(): void` (clears state + timer), `useToast(): ToastSpec | null` (React hook). If using `zustand`, do `create()` without `persist` middleware.

    Write `src/components/Toast.tsx`: functional component with props `{ toast: ToastSpec; onDismiss: () => void }`. Render `<div role="alert" className="fixed bottom-4 inset-x-4 md:inset-x-auto md:right-4 md:max-w-md bg-surface border-l-4 border-danger rounded-md shadow-lg p-4 flex items-start gap-3">` containing a danger-colored icon (`<TriangleAlert />` from lucide-react), a `<div className="flex-1">` with `<p style={{ fontSize: '14px', fontWeight: 700 }}>{toast.title}</p>` and `<p className="text-ink mt-1" style={{ fontSize: '16px', lineHeight: 1.5 }}>{toast.body}</p>`, and a `<button type="button" aria-label="Dismiss" onClick={onDismiss} className="min-w-[44px] min-h-[44px] text-ink-mut">` containing an `<X />` icon.

    Write `src/components/ToastHost.tsx`: a thin wrapper — `const toast = useToast(); if (!toast) return null; return <Toast toast={toast} onDismiss={dismissToast} />;`. Plan 04 will mount `<ToastHost />` inside `<AppShell>` (NOT in this task — keeping App.tsx untouched here so Wave 2 parallel Plan 03 has zero overlap).

    Do NOT mount RoomDropzone or ToastHost into App.tsx in this plan — that wiring is Plan 04 (Wave 3). This task creates the components in isolation. Plan 04 will import them.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm build && grep -q "Upload a photo of your room" src/features/room/RoomDropzone.tsx && grep -q "capture=\"environment\"" src/features/room/RoomDropzone.tsx && grep -q "accept=\"image/\\*\"" src/features/room/RoomDropzone.tsx && grep -q "skeleton-pulse" src/features/room/SkeletonRoom.tsx && grep -q "Loading your room photo" src/features/room/SkeletonRoom.tsx && grep -q "showToast" src/store/toastStore.ts && grep -q "dismissToast" src/store/toastStore.ts && grep -q "useToast" src/components/ToastHost.tsx</automated>
  </verify>
  <done>
    RoomDropzone has hidden input with correct attributes, click + drag/drop both trigger `onFile`; SkeletonRoom pulses at 1.5s; toastStore + Toast + ToastHost exist and can be driven imperatively; typecheck + build pass; locked UI-SPEC copy strings present verbatim.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual mobile-shell smoke test (FND-03 visual + safe-area + camera affordance)</name>
  <what-built>
    Task 1 added the UI-SPEC design tokens to Tailwind v4 via `@theme`, loaded Marcellus + Mulish fonts with display=swap, and declared `100svh` + safe-area padding on `.app-shell`. Task 2 introduced `<AppShell>`, `<Header>` (with conditional `Change room photo` CTA), refactored `<Footer>` to use tokens, and re-shaped `App.tsx` around the new shell while preserving Plan 01's IDB skeleton-ping demo. Task 3 created `<RoomDropzone>` with the locked empty-state copy + camera-capable hidden input + drag/drop handlers, `<SkeletonRoom>` with the 1.5s opacity pulse, and the `<Toast>`/`<ToastHost>` + `toastStore` machinery. RoomDropzone and ToastHost are NOT mounted in `App.tsx` yet — Plan 04 (Wave 3) handles that wiring after Plan 03 ships the pipeline + store.
  </what-built>
  <how-to-verify>
    1. `pnpm dev` → open `http://localhost:5173/`.
    2. Open Chrome DevTools → toggle device emulation → choose **iPhone SE** (375×667). Verify: no horizontal scroll; header `RoomDrop` wordmark visible; tagline `Preview a piece before you buy it` is HIDDEN at this width (it should only appear at md+).
    3. Footer at the bottom reads `RoomDrop is **Open source** under AGPL-3.0` with the link visible.
    4. Toggle device emulation OFF (desktop view) — confirm tagline IS visible next to the wordmark.
    5. Throw the dev URL on a real iPhone (or another mobile device). Confirm: (a) safe-area inset is honored (the header isn't under the notch, footer isn't under the home indicator); (b) the page is full-height with no awkward viewport jump when scrolling.
    6. Inspect the `<RoomDropzone>` component in isolation — easiest path: temporarily render it in App.tsx's `<main>` for the smoke test ONLY, then revert. Confirm: clicking the dropzone opens the file picker; on iOS Safari, tapping it shows the "Take Photo" / "Photo Library" / "Choose File" sheet (the `capture=environment` hint).
    7. Toast manual test: in the browser console run `import('./src/store/toastStore.ts').then(m => m.showToast({ title: 'That photo format isn\\'t supported yet', body: 'iPhone HEIC photos can\\'t be opened in the browser. In Photos, tap Share → "Save to Files" as JPEG, then upload that.', variant: 'error' }))` — confirm a toast appears with `--danger` left accent, title + body, dismiss button works, and auto-dismisses after ~8 seconds.
    8. Confirm SkeletonRoom: temporarily render it; confirm 16:9 box, surface color, pulsing opacity. Revert temporary changes after verifying.
    9. `pnpm build` → `pnpm preview` → confirm the same behaviors work on the production bundle.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g., "tagline shows on mobile", "footer overlapped by home indicator", "drag/drop doesn't tint border", "toast didn't auto-dismiss").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → fonts.googleapis.com + fonts.gstatic.com | Google Fonts CSS + WOFF2 fetched at load — third-party request crosses here |
| User → RoomDropzone (`<input type=file>`) | User-controlled file enters as `File` object; this plan only forwards it to `onFile()` — pipeline normalization is Plan 03/04 |
| User → ToastHost | No untrusted input; toast copy is locked from UI-SPEC and always developer-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure | Google Fonts requests reveal IP + UA to Google | accept | Per RESEARCH §"Security Domain" and UI-SPEC, font loading via fonts.googleapis.com is acceptable for portfolio scope (no user PII transmitted; revisit self-hosting at Phase 6 PWA). |
| T-01-02-02 | Denial of Service | `<input type=file>` accepts any file | accept | This plan only forwards `File` to `onFile` callback. Real validation (HEIC sniff, decode, 2048-px cap) is owned by Plan 03's ImagePipeline (T-01-03-*). |
| T-01-02-03 | XSS via filename | `RoomDropzone` does NOT render `file.name` anywhere | mitigate | Per RESEARCH §"Security Domain → Known Threat Patterns": Phase 1 never renders filenames. Codified by absence — no `{file.name}` JSX in the component. |
| T-01-02-04 | XSS via toast body | `<Toast>` renders `toast.body` as plain text via React | mitigate | React text rendering auto-escapes. Toast copy is developer-controlled (locked UI-SPEC strings only). No `dangerouslySetInnerHTML` anywhere in this plan. |
| T-01-02-05 | Tampering | Tailwind v4 `@theme` token leakage | accept | Tokens are CSS — not secrets. Public design tokens by definition. |
| T-01-02-06 | Information Disclosure | Toast auto-dismiss timer leaks if a new toast preempts old one | mitigate | `showToast` MUST clear any prior `setTimeout` before scheduling the new one — explicit step in Task 3's action. |
</threat_model>

<verification>
- iPhone SE viewport: no horizontal scroll; header tagline hidden; footer visible.
- Safe-area insets honored on a real notch device.
- RoomDropzone hidden input has `type=file accept=image/* capture=environment`.
- RoomDropzone click + drop both fire `onFile`.
- toastStore + Toast + ToastHost work imperatively (manual console test).
- SkeletonRoom pulses at 1.5s with correct aria-label.
- `pnpm build` + `pnpm preview` ship the same behavior.
</verification>

<success_criteria>
- All four tasks complete; checkpoint approved.
- Visual shell matches UI-SPEC tokens (color, typography, spacing) and locked copy.
- Components are export-ready for Plan 04 to wire into App.tsx (`<RoomDropzone onFile={…} />`, `<ToastHost />`).
- No file conflicts with Plan 03 (Wave 2 parallel).
- Plan 01's IDB skeleton-ping demo still runs in `<main>` until Plan 04 replaces it.
</success_criteria>

<output>
Create `.planning/phases/01-foundations-image-pipeline/01-02-SUMMARY.md` documenting:
- Exact Google Fonts URL used.
- Any deviations from UI-SPEC tokens (hex codes that needed adjustment for AA contrast).
- Whether the iPhone SE manual smoke test was performed on real hardware vs DevTools emulation.
- Confirmation that no files in this plan overlap with Plan 03's `files_modified`.
</output>
