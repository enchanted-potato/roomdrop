---
phase: 01-foundations-image-pipeline
plan: 03
type: tdd
wave: 2
depends_on:
  - 01-01
files_modified:
  - vite.config.ts
  - package.json
  - src/lib/image-pipeline/index.ts
  - src/lib/image-pipeline/orientation.ts
  - src/lib/image-pipeline/heic-sniff.ts
  - src/lib/image-pipeline/image-pipeline.test.ts
  - src/lib/idb/blobIds.ts
  - src/store/useAppStore.ts
  - src/store/types.ts
  - src/test/setup.ts
  - src/test/fixtures/portrait-orient-6.jpg
  - src/test/fixtures/heic-magic-bytes.bin
  - src/test/fixtures/landscape-4000x3000.jpg
autonomous: true
requirements:
  - FND-04
  - PER-03
  - PER-04
tags:
  - tdd
  - image-pipeline
  - exif
  - heic
  - persistence
  - zustand
  - idb-keyval
  - vitest

must_haves:
  truths:
    - "ImagePipeline rejects HEIC files by magic-byte sniff before decode (D-02)"
    - "ImagePipeline reads EXIF orientation via exifr (orientation-only import) and applies the rotation manually — never relies on createImageBitmap({imageOrientation:'from-image'}) (D-01, RESEARCH Pitfall 2)"
    - "ImagePipeline resizes long edge to ≤2048 px, preserves aspect, encodes as JPEG quality 0.9 (D-03)"
    - "ImagePipeline returns { blob, width, height, mimeType: 'image/jpeg' } regardless of caller — same signature for rooms (Phase 1) and products (Phase 2)"
    - "ImagePipeline calls bitmap.close() in a finally block to free GPU memory promptly on iOS (RESEARCH Pitfall 1)"
    - "useAppStore has Zustand `persist` middleware with `partialize` excluding blobs and `version: 1` from day one (D-04, D-07)"
    - "Persisted state schema is `{ schemaVersion: 1, rooms: Record<string, Room>, activeRoomId: string | null, libraryItems: Record<string, LibraryItem>, placements: Record<string, Placement[]> }` (D-06)"
    - "All persisted values are JSON-safe (no Date/Map/Set/Blob; timestamps as epoch numbers)"
    - "Library is stored as a top-level `Record<itemId, LibraryItem>` — NOT keyed by room (D-06; supports UPL-03's 'replace room without losing library')"
    - "lib/idb/blobIds.ts exports `roomBlobId(uuid)` and `libBlobId(uuid)` helpers returning correctly-namespaced BlobId values (D-05)"
    - "Vitest with jsdom + fake-indexeddb test setup runs `pnpm vitest run` green (D-13)"
    - "image-pipeline.test.ts covers: (a) EXIF orientation 6 input rotates output by 90°, (b) 4000×3000 input resizes to ≤2048 long edge, (c) HEIC magic bytes throw HeicNotSupportedError"
  artifacts:
    - path: "src/lib/image-pipeline/index.ts"
      provides: "pipeline(file: File | Blob): Promise<NormalizedImage> + HeicNotSupportedError"
      exports: ["pipeline", "HeicNotSupportedError", "NormalizedImage"]
    - path: "src/lib/image-pipeline/heic-sniff.ts"
      provides: "sniffIsHeic(blob): Promise<boolean> — first-12-bytes ftyp brand check"
      exports: ["sniffIsHeic"]
    - path: "src/lib/image-pipeline/orientation.ts"
      provides: "applyOrientationTransform(ctx, orient, w, h) — cases 1..8"
      exports: ["applyOrientationTransform"]
    - path: "src/store/useAppStore.ts"
      provides: "Zustand store with persist middleware (metadata only, version 1)"
      exports: ["useAppStore"]
    - path: "src/store/types.ts"
      provides: "Room, LibraryItem, Placement, PersistedState type definitions"
      exports: ["Room", "LibraryItem", "Placement", "PersistedState"]
    - path: "src/lib/idb/blobIds.ts"
      provides: "Namespaced BlobId helpers (D-05)"
      exports: ["roomBlobId", "libBlobId"]
    - path: "src/lib/image-pipeline/image-pipeline.test.ts"
      provides: "The one meaningful Vitest test mandated by D-13"
      contains: "describe"
  key_links:
    - from: "src/lib/image-pipeline/index.ts"
      to: "src/lib/image-pipeline/heic-sniff.ts"
      via: "if (await sniffIsHeic(file)) throw new HeicNotSupportedError()"
    - from: "src/lib/image-pipeline/index.ts"
      to: "src/lib/image-pipeline/orientation.ts"
      via: "applyOrientationTransform(ctx, orient, dstW, dstH)"
    - from: "src/lib/image-pipeline/index.ts"
      to: "exifr"
      via: "import { orientation as readOrientation } from 'exifr'"
    - from: "src/store/useAppStore.ts"
      to: "zustand/middleware"
      via: "import { persist } from 'zustand/middleware'"
---

<objective>
Implement the **pure logic** layer of Phase 1: the `ImagePipeline` (EXIF orient + HEIC reject + ≤2048-px resize), the `useAppStore` Zustand store with `persist` middleware (metadata-only, version: 1, multi-room schema from day one), the typed `BlobId` helpers under `lib/idb/`, and the Vitest test infrastructure with one meaningful test covering the three FND-04 behaviors.

This plan runs in **Wave 2 parallel** with Plan 02. Its `files_modified` list intersects with **no** Plan 02 files except `vite.config.ts` and `package.json`. The two `vite.config.ts` edits do conflict — therefore Plan 03's edit to `vite.config.ts` is scoped to **adding the `test.setupFiles` entry only**; Plan 02 does not touch `vite.config.ts`. `package.json` edit is adding test deps only (no script changes — Plan 01 already created the `test` script).

This plan uses TDD: write the image-pipeline test first (RED), implement to make it pass (GREEN). The store + idb helpers are simple enough to ship without dedicated unit tests in Phase 1 per D-13 ("ONE meaningful test").

Purpose: deliver the seams Plan 04 wires together — `pipeline(file)`, `useAppStore.setActiveRoom`, `roomBlobId(uuid)`.
Output: TypeScript modules that compile, lint, and pass the single mandated Vitest test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundations-image-pipeline/01-CONTEXT.md
@.planning/phases/01-foundations-image-pipeline/01-RESEARCH.md
@.planning/phases/01-foundations-image-pipeline/01-VALIDATION.md
@.planning/phases/01-foundations-image-pipeline/SKELETON.md
@.planning/phases/01-foundations-image-pipeline/01-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Contracts this plan defines for Plan 04 + later phases. -->

From src/lib/image-pipeline/index.ts:
```typescript
export interface NormalizedImage {
  blob: Blob;
  width: number;
  height: number;
  mimeType: 'image/jpeg';   // Phase 1 always emits JPEG (D-03)
}
export class HeicNotSupportedError extends Error {}
export function pipeline(file: File | Blob): Promise<NormalizedImage>;
```

From src/store/types.ts:
```typescript
export interface Room { id: string; blobId: string; width: number; height: number; createdAt: number; }
export interface LibraryItem { id: string; originalBlobId: string; cutoutBlobId: string | null; createdAt: number; }
export interface Placement { /* Phase 2 fills */ }
export interface PersistedState {
  schemaVersion: 1;
  rooms: Record<string, Room>;
  activeRoomId: string | null;
  libraryItems: Record<string, LibraryItem>;
  placements: Record<string, Placement[]>;
}
```

From src/store/useAppStore.ts:
```typescript
export const useAppStore: UseBoundStore<...>;
// Actions defined: setActiveRoom(room: Room): void; clearActiveRoom(): void; addLibraryItem(item: LibraryItem): void;
// Phase 2 will add: deleteLibraryItem, addPlacement, updatePlacement, etc.
```

From src/lib/idb/blobIds.ts:
```typescript
export function roomBlobId(uuid: string): BlobId & `room:${string}`;
export function libBlobId(uuid: string): BlobId & `lib:${string}`;
```

<!-- Plan 01 already shipped src/lib/idb/index.ts with setBlob/getBlob/deleteBlob/skeletonPing + BlobId type. -->
<!-- This plan ADDS src/lib/idb/blobIds.ts as a sibling helper file (no edit to Plan 01's index.ts). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install + configure Vitest jsdom env, fake-indexeddb, jest-dom matchers, test fixtures (Wave 0 for FND-04)</name>
  <files>package.json, vite.config.ts, src/test/setup.ts, src/test/fixtures/portrait-orient-6.jpg, src/test/fixtures/heic-magic-bytes.bin, src/test/fixtures/landscape-4000x3000.jpg</files>
  <read_first>
    - vite.config.ts (Plan 01 baseline — already has `test: { environment: 'jsdom', globals: true }`; this task adds `setupFiles`)
    - .planning/phases/01-foundations-image-pipeline/01-VALIDATION.md §"Wave 0 Requirements" (vitest deps + fixtures list)
    - .planning/phases/01-foundations-image-pipeline/01-RESEARCH.md §"Validation Architecture" (test framework: vitest + jsdom; planner may swap to happy-dom if jsdom is insufficient for createImageBitmap/OffscreenCanvas)
    - .planning/phases/01-foundations-image-pipeline/01-CONTEXT.md (D-13: ONE meaningful test only)
  </read_first>
  <behavior>
    - Test 1: `package.json` devDependencies contains `@testing-library/react`, `@testing-library/jest-dom`, `jsdom`, `fake-indexeddb` (the four Wave 0 deps from VALIDATION.md)
    - Test 2: `vite.config.ts` `test` block has `setupFiles: ['./src/test/setup.ts']` and `environment: 'jsdom'`
    - Test 3: `src/test/setup.ts` imports `@testing-library/jest-dom/vitest`, registers `fake-indexeddb/auto`, and calls `afterEach(() => cleanup())` from `@testing-library/react`
    - Test 4: All three fixture files exist: `portrait-orient-6.jpg` (real JPEG with EXIF orientation tag = 6), `heic-magic-bytes.bin` (raw 12-byte file: `00 00 00 18 66 74 79 70 68 65 69 63` representing `[len][ftyp][heic]`), `landscape-4000x3000.jpg` (real JPEG at 4000×3000)
    - Test 5: `pnpm vitest run` exits 0 (still no tests at this point — file is added in Task 2)
  </behavior>
  <action>
    Install dev deps: `pnpm add -D @testing-library/react @testing-library/jest-dom jsdom fake-indexeddb`. Commit the updated `pnpm-lock.yaml`. All four packages are well-known [OK]-classified test infrastructure (none flagged by slopcheck heuristics).

    Update `vite.config.ts`: extend the existing `test` block to add `setupFiles: ['./src/test/setup.ts']`. Keep `environment: 'jsdom'`, `globals: true`. Do NOT add a `coverage` config (out of scope for Phase 1).

    Write `src/test/setup.ts`: at the top `import '@testing-library/jest-dom/vitest';` (registers DOM matchers), `import 'fake-indexeddb/auto';` (registers a synthetic IndexedDB for the test process — required because jsdom has no IDB), then `import { afterEach } from 'vitest'; import { cleanup } from '@testing-library/react'; afterEach(() => cleanup());`.

    Create `src/test/fixtures/heic-magic-bytes.bin`: a 12-byte file containing the bytes `00 00 00 18 66 74 79 70 68 65 69 63`. Generate it with Node: `node -e "require('fs').writeFileSync('src/test/fixtures/heic-magic-bytes.bin', Buffer.from([0,0,0,0x18,0x66,0x74,0x79,0x70,0x68,0x65,0x69,0x63]))"`. This is the smallest possible file that satisfies the D-02 magic-byte sniff.

    For `portrait-orient-6.jpg`: download or generate a small JPEG with EXIF orientation tag = 6. Source option A: download the canonical EXIF-orientation test set from `https://github.com/recurser/exif-orientation-examples` (Landscape_6.jpg, ~5 KB). Source option B: synthesize via `sharp` in a one-shot Node script (do NOT add `sharp` as a project dep; run via `pnpm dlx sharp-cli` or copy a public test fixture). The file must be small (<50 KB) and have orientation=6 in its EXIF header.

    For `landscape-4000x3000.jpg`: generate via `pnpm dlx sharp-cli resize 4000 3000 ...` from any public-domain image, or embed a tiny `1x1` solid color JPEG re-encoded at 4000×3000 (will compress to ~30 KB). The exact image content does NOT matter — only the dimensions matter for the resize assertion.

    Per CONTEXT D-13: do NOT add Playwright, do NOT add component-level tests for App.tsx/Header.tsx/Footer.tsx — only the one image-pipeline test is in scope.
  </action>
  <verify>
    <automated>pnpm install && grep -q "@testing-library/react" package.json && grep -q "fake-indexeddb" package.json && grep -q "setupFiles" vite.config.ts && test -f src/test/setup.ts && test -f src/test/fixtures/heic-magic-bytes.bin && test -f src/test/fixtures/portrait-orient-6.jpg && test -f src/test/fixtures/landscape-4000x3000.jpg && pnpm vitest run</automated>
  </verify>
  <done>
    Vitest jsdom env is configured with jest-dom matchers + fake-indexeddb auto-registration; all three fixture files exist with the correct shape; `pnpm vitest run` exits 0 with zero tests collected.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD ImagePipeline — RED test, then GREEN implementation (HEIC sniff + EXIF orient + ≤2048 resize)</name>
  <files>src/lib/image-pipeline/heic-sniff.ts, src/lib/image-pipeline/orientation.ts, src/lib/image-pipeline/index.ts, src/lib/image-pipeline/image-pipeline.test.ts</files>
  <read_first>
    - src/test/setup.ts (Task 1 of this plan — jsdom + fake-indexeddb + jest-dom matchers)
    - src/test/fixtures/ (Task 1 of this plan — the three fixture files)
    - .planning/phases/01-foundations-image-pipeline/01-CONTEXT.md (D-01 exifr-only-orient + createImageBitmap + OffscreenCanvas; D-02 magic-byte sniff with all four brands `ftypheic/heix/hevc/mif1`; D-03 ≤2048 long edge, JPEG quality 0.9)
    - .planning/phases/01-foundations-image-pipeline/01-RESEARCH.md §"Code Examples → Image pipeline shape" (reference implementation), §"Common Pitfalls" Pitfall 1 (bitmap.close() in finally) + Pitfall 2 (do NOT use imageOrientation option) + Pitfall 3 (sniff before decode)
    - .planning/phases/01-foundations-image-pipeline/01-VALIDATION.md §"Per-Task Verification Map" → FND-04 row (test command: `pnpm vitest run src/lib/image-pipeline/image-pipeline.test.ts`)
  </read_first>
  <behavior>
    - Test 1 (RED→GREEN): Given an empty Blob → `sniffIsHeic(blob)` resolves to `false`
    - Test 2 (RED→GREEN): Given the 12-byte `heic-magic-bytes.bin` fixture loaded as a Blob → `sniffIsHeic(blob)` resolves to `true`
    - Test 3 (RED→GREEN): Given each of the four brand bytes (`heic`, `heix`, `hevc`, `mif1`) at offset 8..12 with `ftyp` at offset 4..8 → `sniffIsHeic` returns `true`. Given `ftyp` + brand `mp4 ` → returns `false`. Parameterize with `it.each`.
    - Test 4 (RED→GREEN): `pipeline(heicBlob)` rejects with an instance of `HeicNotSupportedError`
    - Test 5 (RED→GREEN): `pipeline(landscape-4000x3000.jpg)` resolves to `{ blob, width, height, mimeType: 'image/jpeg' }` where `Math.max(width, height) === 2048` and `Math.min(width, height) === Math.round(2048 * 3000 / 4000)` (i.e. 1536)
    - Test 6 (RED→GREEN): `pipeline(portrait-orient-6.jpg)` resolves to a NormalizedImage whose width/height correspond to the EXIF-rotated dimensions (a portrait phone photo with orient=6 has its raw pixels in landscape orientation; after EXIF correction the output width < height). Assert `result.height > result.width` after rotation, OR — if the fixture's raw pixel dimensions happen to already be portrait — assert that calling `pipeline` on a fixture WITHOUT orientation tag gives a different output dimension order than with orientation=6.
    - Test 7 (RED→GREEN): `applyOrientationTransform` for cases 1..8 applies the documented CSS/canvas transform sequences (smoke test by spying on a mock `OffscreenCanvasRenderingContext2D` and asserting the right calls happen — translate/rotate/scale).
  </behavior>
  <action>
    **RED phase:** Write `src/lib/image-pipeline/image-pipeline.test.ts` FIRST with the seven `describe`/`it` blocks above. Import `pipeline`, `HeicNotSupportedError`, `sniffIsHeic`, `applyOrientationTransform` from the still-to-exist source files. Use `import { readFileSync } from 'node:fs'` + `new Blob([readFileSync('src/test/fixtures/...')])` to load fixtures. For the mock context test, build a minimal `Partial<OffscreenCanvasRenderingContext2D>` with `vi.fn()` for `translate/scale/rotate` and assert call ordering for each orientation case. Commit: `test(01-03): add failing tests for image pipeline (FND-04)`. Confirm `pnpm vitest run src/lib/image-pipeline/image-pipeline.test.ts` reports 7 failing tests (modules not found).

    **GREEN phase:** Implement the three source files.

    `src/lib/image-pipeline/heic-sniff.ts`: export `const HEIC_BRANDS = new Set(['heic', 'heix', 'hevc', 'mif1']);` and `export async function sniffIsHeic(file: Blob): Promise<boolean>`. Implementation: read first 12 bytes via `file.slice(0, 12).arrayBuffer()`. If file size < 12 return false. Check bytes 4..8 === ASCII `ftyp`; if not return false. Read bytes 8..12 as ASCII brand; return `HEIC_BRANDS.has(brand)`. Use `String.fromCharCode(...new Uint8Array(buf).slice(i, j))` per RESEARCH Code Examples.

    `src/lib/image-pipeline/orientation.ts`: export `applyOrientationTransform(ctx: OffscreenCanvasRenderingContext2D, orient: number, w: number, h: number): void`. Copy the switch statement from RESEARCH §"Code Examples → Image pipeline shape" verbatim — it correctly covers all 8 EXIF orientations.

    `src/lib/image-pipeline/index.ts`: export `interface NormalizedImage { blob: Blob; width: number; height: number; mimeType: 'image/jpeg' }`, `export class HeicNotSupportedError extends Error { constructor() { super('HEIC not supported'); this.name = 'HeicNotSupportedError'; } }`, and `export async function pipeline(file: File | Blob): Promise<NormalizedImage>`. Implementation per RESEARCH Code Examples:
    1. `if (await sniffIsHeic(file)) throw new HeicNotSupportedError();` (per D-02 — sniff BEFORE decode)
    2. `const orient = (await readOrientation(file)) ?? 1;` (import via `import { orientation as readOrientation } from 'exifr';`)
    3. `const bitmap = await createImageBitmap(file);` (do NOT pass `{ imageOrientation: 'from-image' }` per RESEARCH Pitfall 2)
    4. Use a `try { ... } finally { bitmap.close(); }` block (per RESEARCH Pitfall 1 — iOS memory hygiene)
    5. Compute `swapAxes = orient >= 5`, `srcW = bitmap.width`, `srcH = bitmap.height`, `longEdge = Math.max(srcW, srcH)`, `scale = longEdge > 2048 ? 2048 / longEdge : 1`, `dstW = Math.round(srcW * scale)`, `dstH = Math.round(srcH * scale)`, `outW = swapAxes ? dstH : dstW`, `outH = swapAxes ? dstW : dstH`
    6. Create `const canvas = new OffscreenCanvas(outW, outH)`, get its 2d context with `imageSmoothingQuality: 'high'`, call `applyOrientationTransform(ctx, orient, dstW, dstH)`, then `ctx.drawImage(bitmap, 0, 0, dstW, dstH)`
    7. `const blob = await canvas.convertToBlob({ type: 'image/jpeg', quality: 0.9 });`
    8. Return `{ blob, width: outW, height: outH, mimeType: 'image/jpeg' }`

    **Note on jsdom limits:** jsdom may not implement `OffscreenCanvas` / `createImageBitmap` natively. If the GREEN-phase tests fail because of missing browser APIs, swap to `happy-dom` (per RESEARCH §"Validation Architecture" — planner may swap) OR install `canvas` + register polyfills in `src/test/setup.ts`. Document the choice in the SUMMARY. The exifr import + HEIC sniff tests work in pure jsdom regardless; only the resize + orient test needs the canvas environment.

    Constant `MAX_EDGE = 2048` declared as a module-level const for clarity. Do NOT export it (Phase 4 will introduce a separate cap for product images; keeping `pipeline()` opaque preserves the seam).

    Commit GREEN: `feat(01-03): implement image pipeline (HEIC sniff + EXIF orient + resize) — FND-04`.

    Do NOT add a `bgRemoval` module, do NOT add Konva, do NOT touch `src/store/*` (separate task), do NOT integrate with `useAppStore`.
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/image-pipeline/image-pipeline.test.ts</automated>
  </verify>
  <done>
    `pnpm vitest run src/lib/image-pipeline/image-pipeline.test.ts` exits 0 with all 7 tests green; ImagePipeline rejects HEIC magic bytes before any decode; resizes a 4000×3000 input down to a 2048-long-edge JPEG; EXIF orientation 6 correctly produces a rotated output; bitmap.close() is called in a finally block.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Implement useAppStore with Zustand persist (metadata-only, version: 1, multi-room schema) + types + BlobId helpers</name>
  <files>src/store/types.ts, src/store/useAppStore.ts, src/lib/idb/blobIds.ts</files>
  <read_first>
    - src/lib/idb/index.ts (Plan 01 baseline — already exports `BlobId` template-literal type and `setBlob`/`getBlob`/`deleteBlob`/`skeletonPing`)
    - .planning/phases/01-foundations-image-pipeline/01-CONTEXT.md (D-04 persist middleware + partialize; D-05 idb-keyval blob namespaces; D-06 multi-room schema as `Record<roomId, Room>`; D-07 schemaVersion 1 from day one; Claude's Discretion — must round-trip through JSON; use crypto.randomUUID)
    - .planning/phases/01-foundations-image-pipeline/01-RESEARCH.md §"Code Examples → Zustand store with partialize, version, and onRehydrateStorage" (reference implementation), §"Multi-Room Schema Seam" (full TypeScript shape)
  </read_first>
  <behavior>
    - Test 1: `src/store/types.ts` exports `Room`, `LibraryItem`, `Placement`, `PersistedState`. `Room` has `{ id: string; blobId: string; width: number; height: number; createdAt: number; }`. `LibraryItem` has `{ id: string; originalBlobId: string; cutoutBlobId: string | null; createdAt: number; }` (cutoutBlobId nullable so the `cutoutId ?? originalId` seam works pre-Phase-4). `Placement` is an empty interface (Phase 2 fills). `PersistedState` matches CONTEXT D-06 verbatim.
    - Test 2: `useAppStore` initial state has `schemaVersion: 1`, `rooms: {}`, `activeRoomId: null`, `libraryItems: {}`, `placements: {}`
    - Test 3: `useAppStore` `persist` config has `name: 'roomdrop'`, `version: 1`, and a `partialize` function that returns `{ schemaVersion, rooms, activeRoomId, libraryItems, placements }` (NO action functions, NO blobs)
    - Test 4: `useAppStore.getState().setActiveRoom({ id: 'r1', blobId: 'room:abc', width: 100, height: 80, createdAt: 0 })` updates `rooms['r1']` and `activeRoomId`
    - Test 5: `useAppStore.getState().clearActiveRoom()` sets `activeRoomId: null` but leaves the `rooms` record intact
    - Test 6: All persisted values pass a JSON round-trip — `JSON.parse(JSON.stringify(persistedState))` returns an equal object (no Date/Map/Set/Blob in state)
    - Test 7: `roomBlobId('abc')` returns `'room:abc'` typed as `BlobId & \`room:${string}\``; `libBlobId('xyz')` returns `'lib:xyz'` typed as `BlobId & \`lib:${string}\``
  </behavior>
  <action>
    Write `src/store/types.ts`: export the four interfaces above. Use `Placement` as `export interface Placement {}` with a `// Phase 2 will populate this — kept empty to preserve schema seam` comment. Per D-06, the `PersistedState` shape MUST match exactly: `{ schemaVersion: 1; rooms: Record<string, Room>; activeRoomId: string | null; libraryItems: Record<string, LibraryItem>; placements: Record<string, Placement[]>; }`.

    Write `src/lib/idb/blobIds.ts`: import `BlobId` from `./index`. Export `roomBlobId(uuid: string)` that returns `\`room:${uuid}\` as BlobId & \`room:${string}\``. Export `libBlobId(uuid: string)` symmetrically. These helpers are thin but enforce namespace discipline — Phase 2 will call `libBlobId(crypto.randomUUID())` exactly once per library upload.

    Write `src/store/useAppStore.ts`: import `create` from `zustand`, `persist` from `zustand/middleware`, and the types from `./types`. Mirror RESEARCH §"Code Examples → Zustand store" implementation. Actions in Phase 1 scope:
    - `setActiveRoom(room: Room): void` — `set((s) => ({ rooms: { ...s.rooms, [room.id]: room }, activeRoomId: room.id }))`
    - `clearActiveRoom(): void` — `set({ activeRoomId: null })` (does NOT delete the room record — keeps the multi-room seam; Plan 04 calls this in the D-09 self-healing path)
    - `addLibraryItem(item: LibraryItem): void` — `set((s) => ({ libraryItems: { ...s.libraryItems, [item.id]: item } }))` (declared for Plan 04 + Phase 2 use; verifies UPL-03 "library survives room replace" because library is a top-level slice, NOT keyed by roomId)

    `persist` config: `{ name: 'roomdrop', version: 1, partialize: (s) => ({ schemaVersion: s.schemaVersion, rooms: s.rooms, activeRoomId: s.activeRoomId, libraryItems: s.libraryItems, placements: s.placements }) }`. Per D-04 the partialize MUST exclude actions (Zustand's default would persist them as `undefined` via JSON, but explicit partialize is clearer + future-proofed). Do NOT add a `migrate` function — D-07 says version: 1 is the floor; migrate is a Phase 5+ concern.

    Persisted-payload schema is ALSO the contract behind PER-04 (multi-room schema in v1 even though UI is single-room). Document this with a `/** Multi-room schema — D-06. v1 UI only exposes one room but the shape supports many. */` JSDoc on the `rooms` field in `types.ts`.

    Round-trip JSON safety (per CONTEXT Claude's Discretion + RESEARCH §"Multi-Room Schema Seam"): `createdAt` MUST be `number` (epoch ms via `Date.now()`), NOT `Date`. No `Map`, `Set`, or `Blob` anywhere in persisted state.

    Do NOT add `onRehydrateStorage` callbacks here (Plan 04 will add the boot-time hydration sequence). Do NOT call `navigator.storage.persist()` here (Plan 04). Do NOT mount the store anywhere — App.tsx is Plan 04's responsibility.

    No dedicated unit test required for this task per D-13. The TypeScript compiler (`pnpm typecheck`) + the schema test inside the image-pipeline test file are sufficient to gate this work. If you want a quick smoke, add 1–2 assertions inside `image-pipeline.test.ts` (e.g. `expect(useAppStore.getState().schemaVersion).toBe(1)`) — optional.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm vitest run && grep -q "version: 1" src/store/useAppStore.ts && grep -q "partialize" src/store/useAppStore.ts && grep -q "schemaVersion: 1" src/store/useAppStore.ts && grep -q "cutoutBlobId: string | null" src/store/types.ts && grep -q "Record<string, Room>" src/store/types.ts && grep -q "roomBlobId" src/lib/idb/blobIds.ts</automated>
  </verify>
  <done>
    `useAppStore` + types compile with `strict: true` + `noUncheckedIndexedAccess: true`; persist middleware uses `name: 'roomdrop'`, `version: 1`, `partialize` excluding blobs; multi-room schema seam present; `cutoutId ?? originalId` field declared; `roomBlobId` / `libBlobId` helpers exported.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-supplied File → ImagePipeline | All untrusted bytes enter here; validation must happen before any expensive decode |
| ImagePipeline → IndexedDB (via Plan 04) | Normalized blob is what Plan 04 will persist; size capped by 2048-px output |
| useAppStore → localStorage | Zustand `persist` JSON-serializes the partialize output; bounded by localStorage's ~5 MB cap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Denial of Service | `pipeline(file)` decoding a maliciously oversized or malformed image | mitigate | Per RESEARCH Pitfall 1 + ASVS V5: (a) HEIC sniff rejects before decode (avoids libheif-class memory blowups even if browser decodes them in future), (b) `createImageBitmap` decode failures throw → caller catches + shows generic upload error toast (Plan 04), (c) output canvas is capped at ≤2048×2048 RGBA ≈ 16.7 MB which is below iOS Safari's per-canvas 16.7 Mpx limit, (d) `bitmap.close()` in finally block frees GPU memory promptly. Hostile EXIF with garbage orientation values → `applyOrientationTransform` switch falls through to identity transform (`orient ?? 1` default in source). |
| T-01-03-02 | Tampering | HEIC masquerading as JPEG (iOS Safari lies about `file.type` and extension) | mitigate | D-02 magic-byte sniff of first 12 bytes — never trust `file.type` or extension. Test 2 + Test 3 cover all four ISO/BMFF brands. |
| T-01-03-03 | Information Disclosure | EXIF data in original file may contain GPS / device metadata | mitigate | Pipeline re-encodes via `canvas.convertToBlob({ type: 'image/jpeg' })` which produces a fresh JPEG with NO EXIF metadata. Original file is never persisted — only the canvas output. |
| T-01-03-04 | Tampering | Persisted-state corruption (localStorage hand-edited or version mismatch) | mitigate | D-07 `version: 1` from day one; Zustand `persist` honors `version` on rehydrate. Plan 04 owns the self-healing flow when corrupted state leads to a missing IDB blob (D-09). |
| T-01-03-05 | DoS | localStorage quota exceeded (RESEARCH Pitfall 7) | accept | Phase 1 partialize is minimal (no thumbnails, no data URLs); 5 MB cap is far above what Phase 1 metadata can produce. Plan 04 wraps `setBlob` calls in try/catch for IDB quota; localStorage quota is monitored as a future-phase concern. |
| T-01-03-06 | Spoofing | A test-fixture file masquerading as a real EXIF JPEG | accept | Fixtures are committed sources in `src/test/fixtures/` and reviewed at PR time; not a runtime concern. |
| T-01-03-07 | Tampering | Zustand state mutation outside of `set()` | mitigate | Strict TypeScript + `useAppStore.getState()` returning a frozen-by-convention object; Phase 2 will tighten with immer if needed. |
</threat_model>

<verification>
- `pnpm vitest run` exits 0 with the image-pipeline test green (FND-04).
- `pnpm typecheck` passes with `strict: true` + `noUncheckedIndexedAccess: true`.
- `useAppStore` persists metadata only (`partialize`, `version: 1`).
- Multi-room schema present in v1 (PER-04 satisfied at schema level).
- BlobId helpers correctly namespace `room:` and `lib:`.
- No file-level conflicts with Plan 02 — only `vite.config.ts` (extending the `test` block) and `package.json` (adding 4 test deps) are touched; Plan 02 does not modify these files.
</verification>

<success_criteria>
- All three tasks complete (no checkpoint — this is pure logic; checkpoint lives in Plan 04 which wires everything).
- `pnpm vitest run` exits 0 with 7+ tests green.
- TypeScript signatures match the `<interfaces>` block above so Plan 04 can wire without rework.
- `bitmap.close()` confirmed in source (grep) and `mimeType: 'image/jpeg'` confirmed for output.
- AppShell/Header/Dropzone code (Plan 02) was NOT touched.
</success_criteria>

<output>
Create `.planning/phases/01-foundations-image-pipeline/01-03-SUMMARY.md` documenting:
- Whether jsdom alone was sufficient for the canvas tests, or whether happy-dom / `canvas` polyfill was needed.
- Source of the EXIF-orientation-6 test fixture (downloaded URL or generation script).
- Final bundle delta from adding exifr (verify via `pnpm build --report` or `rollup-plugin-visualizer` if convenient; otherwise report the `dist/assets/*.js` size delta).
- Confirmation that `pnpm vitest run` reports exactly the tests written in Task 2.
- Any deviation from RESEARCH §"Code Examples" reference implementations.
</output>
