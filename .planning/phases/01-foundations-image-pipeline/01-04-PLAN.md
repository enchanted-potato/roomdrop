---
phase: 01-foundations-image-pipeline
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - src/app/App.tsx
  - src/app/AppShell.tsx
  - src/main.tsx
  - src/features/room/RoomCanvas.tsx
  - src/features/room/useRoomUpload.ts
autonomous: false
requirements:
  - FND-04
  - UPL-01
  - UPL-03
  - PER-02
  - PER-03
tags:
  - integration
  - hydration
  - persistence
  - upload-flow

must_haves:
  truths:
    - "User can upload a room photo via the file picker; on success it appears in the stage and persists across reload (UPL-01, PER-02)"
    - "Boot sequence: Zustand rehydrates synchronously → if activeRoomId set, RoomCanvas paints SkeletonRoom immediately → useEffect reads IDB blob → URL.createObjectURL → swap to <img> with 150ms opacity cross-fade (D-08, PER-02)"
    - "Every URL.createObjectURL output is paired with URL.revokeObjectURL on unmount AND on photo replace (RESEARCH Pitfall 4)"
    - "On IDB read failure (missing blob, store empty), the app self-heals: clears activeRoomId, renders dropzone, emits the locked self-healing toast (D-09)"
    - "On HEIC upload, RoomDropzone's onFile handler catches HeicNotSupportedError and emits the locked HEIC toast (UI-SPEC §Copywriting)"
    - "On any other ImagePipeline failure, the locked generic upload-error toast is emitted (UI-SPEC §Copywriting)"
    - "Header's 'Change room photo' button is wired to re-open the picker; new upload silently replaces the active photo (no destructive confirm in Phase 1; that's Phase 3) (UPL-03)"
    - "Replacing a room photo does NOT clear `useAppStore.libraryItems` (UPL-03)"
    - "Old room's blob is deleted from IDB when replaced (memory hygiene)"
    - "`navigator.storage.persist()` is called once at app boot to dampen iOS 7-day ITP eviction (RESEARCH Pitfall 5 + Open Question #3)"
    - "ToastHost is mounted inside AppShell so errors render globally"
    - "Plan 01's skeletonPing demo is removed from App.tsx (production code only)"
    - "`pnpm build` succeeds; `pnpm preview` serves a working end-to-end app that round-trips reload (manual smoke)"
  artifacts:
    - path: "src/features/room/RoomCanvas.tsx"
      provides: "Loaded-state component: skeleton-first hydration → IDB read → cross-fade <img>"
      exports: ["RoomCanvas"]
    - path: "src/features/room/useRoomUpload.ts"
      provides: "Hook encapsulating: pipeline(file) → setBlob → setActiveRoom → cleanup old blob → emit toast on error"
      exports: ["useRoomUpload"]
    - path: "src/app/App.tsx"
      provides: "Wires AppShell + Header + RoomDropzone/RoomCanvas branch + ToastHost + boot lifecycle"
      contains: "useAppStore"
  key_links:
    - from: "src/features/room/useRoomUpload.ts"
      to: "src/lib/image-pipeline/index.ts"
      via: "pipeline(file)"
    - from: "src/features/room/useRoomUpload.ts"
      to: "src/lib/idb/index.ts + src/lib/idb/blobIds.ts"
      via: "setBlob(roomBlobId(uuid), normalized.blob)"
    - from: "src/features/room/useRoomUpload.ts"
      to: "src/store/useAppStore.ts"
      via: "useAppStore.getState().setActiveRoom(room)"
    - from: "src/features/room/useRoomUpload.ts"
      to: "src/store/toastStore.ts"
      via: "showToast on HeicNotSupportedError + generic decode failure"
    - from: "src/features/room/RoomCanvas.tsx"
      to: "src/lib/idb/index.ts"
      via: "getBlob(room.blobId) in useEffect; revokeObjectURL on cleanup"
    - from: "src/app/App.tsx"
      to: "navigator.storage.persist()"
      via: "useEffect at root mount"
---

<objective>
Integration plan — wire the Plan 02 visual shell and the Plan 03 logic seams into a working end-to-end Phase 1 app. After this plan, a user can upload a room photo, see it appear, replace it without losing the (empty in v1) library, reload the page, and watch the photo re-appear at first paint via skeleton-first hydration with a 150 ms cross-fade.

This plan delivers the **vertical-slice happy path** that the phase user story demands: real upload → real pipeline → real IDB persistence → real cross-fade hydration → real error toasts → real self-healing on data loss.

Purpose: turn separate moving parts into a deploy-ready app that satisfies all five Phase 1 success criteria from ROADMAP.md.
Output: a `dist/` that, when loaded as a static site, fulfills the entire Phase 1 goal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundations-image-pipeline/01-CONTEXT.md
@.planning/phases/01-foundations-image-pipeline/01-RESEARCH.md
@.planning/phases/01-foundations-image-pipeline/01-UI-SPEC.md
@.planning/phases/01-foundations-image-pipeline/01-VALIDATION.md
@.planning/phases/01-foundations-image-pipeline/SKELETON.md
@.planning/phases/01-foundations-image-pipeline/01-01-PLAN.md
@.planning/phases/01-foundations-image-pipeline/01-02-PLAN.md
@.planning/phases/01-foundations-image-pipeline/01-03-PLAN.md
@CLAUDE.md

<interfaces>
<!-- All consumed contracts are already shipped by Plans 01–03. -->
<!-- Plan 02 ships: <AppShell>, <Header hasActiveRoom onChangeRoom>, <RoomDropzone onFile>, <SkeletonRoom>, <ToastHost>, <Toast>, toastStore (showToast, useToast, dismissToast). -->
<!-- Plan 03 ships: pipeline(file), HeicNotSupportedError, NormalizedImage, useAppStore (setActiveRoom, clearActiveRoom, addLibraryItem), Room/LibraryItem/Placement/PersistedState types, roomBlobId / libBlobId. -->
<!-- Plan 01 ships: setBlob, getBlob, deleteBlob, skeletonPing, BlobId type. -->

This plan creates:

From src/features/room/useRoomUpload.ts:
```typescript
export function useRoomUpload(): {
  uploadRoom: (file: File) => Promise<void>;
};
```

From src/features/room/RoomCanvas.tsx:
```typescript
export function RoomCanvas(): JSX.Element | null;
// Subscribes to useAppStore.activeRoomId + rooms[activeRoomId].
// Renders <SkeletonRoom> until IDB read resolves, then <img> with 150ms cross-fade.
// Self-heals (clears activeRoomId + emits toast) when IDB blob is missing.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build useRoomUpload hook (pipeline + setBlob + setActiveRoom + cleanup-old-blob + error toasts)</name>
  <files>src/features/room/useRoomUpload.ts</files>
  <read_first>
    - src/lib/image-pipeline/index.ts (Plan 03 — pipeline, HeicNotSupportedError, NormalizedImage)
    - src/store/useAppStore.ts (Plan 03 — setActiveRoom action shape)
    - src/lib/idb/index.ts (Plan 01 — setBlob, deleteBlob)
    - src/lib/idb/blobIds.ts (Plan 03 — roomBlobId helper)
    - src/store/toastStore.ts (Plan 02 — showToast API)
    - .planning/phases/01-foundations-image-pipeline/01-CONTEXT.md (D-05 idb-keyval blob namespaces; D-06 multi-room schema; UPL-03 replace room without losing library)
    - .planning/phases/01-foundations-image-pipeline/01-UI-SPEC.md §"Copywriting Contract" (exact HEIC + generic upload error copy)
  </read_first>
  <behavior>
    - Test 1: Calling uploadRoom(file) with a valid JPEG → calls pipeline(file) → calls setBlob(roomBlobId(uuid), normalized.blob) → calls useAppStore.getState().setActiveRoom({ id: uuid, blobId: 'room:' + uuid, width, height, createdAt: Date.now() })
    - Test 2: If a prior activeRoomId exists, after the new room is set the prior room's blob is deleted from IDB via deleteBlob(priorRoom.blobId) AND the prior room record is removed from useAppStore.rooms (memory hygiene — multi-room schema kept simple in v1 by not retaining replaced rooms)
    - Test 3: After replace, useAppStore.libraryItems is unchanged (UPL-03)
    - Test 4: If pipeline(file) throws HeicNotSupportedError, showToast({ title: 'That photo format isn\\'t supported yet', body: 'iPhone HEIC photos can\\'t be opened in the browser. In Photos, tap Share → "Save to Files" as JPEG, then upload that.', variant: 'error' }) is called and useAppStore is NOT mutated
    - Test 5: If pipeline(file) throws any other error (decode failure, OOM, etc.), showToast({ title: 'Couldn\\'t open that photo', body: 'The file may be corrupted or too large. Try a different photo.', variant: 'error' }) is called
    - Test 6: If setBlob throws (IDB QuotaExceededError), the same generic error toast is emitted and useAppStore is not mutated
  </behavior>
  <action>
    Write `src/features/room/useRoomUpload.ts`. The "hook" can be a thin React hook OR a pure factory — since the upload handler does not need React state itself, prefer exporting a plain function `uploadRoom` AND a hook `useRoomUpload()` that returns `{ uploadRoom }`. Keeping it as a hook leaves a seam for Phase 2 which will likely want loading state.

    Imports: `pipeline` and `HeicNotSupportedError` from `'../../lib/image-pipeline'`; `setBlob` and `deleteBlob` from `'../../lib/idb'`; `roomBlobId` from `'../../lib/idb/blobIds'`; `useAppStore` from `'../../store/useAppStore'`; `showToast` from `'../../store/toastStore'`; the `Room` type from `'../../store/types'`.

    Define module-level constants `HEIC_TOAST` and `GENERIC_UPLOAD_TOAST` holding the locked UI-SPEC copy verbatim (single source of truth for both this hook and any other surface that needs to emit the same toast). Use the exact strings from UI-SPEC §"Copywriting Contract".

    Implement `async function uploadRoom(file: File): Promise<void>`:
    1. Snapshot prior state via `const prior = useAppStore.getState();` and derive `const priorRoom = prior.activeRoomId ? prior.rooms[prior.activeRoomId] : null;`.
    2. Wrap the pipeline call in try/catch. On catch, branch: if `err instanceof HeicNotSupportedError` call `showToast(HEIC_TOAST)`, else call `showToast(GENERIC_UPLOAD_TOAST)`. Return early.
    3. Generate `const id = crypto.randomUUID();` and `const blobId = roomBlobId(id);`.
    4. Persist the blob via `setBlob(blobId, normalized.blob)`. Wrap in try/catch — on catch emit `GENERIC_UPLOAD_TOAST` and return.
    5. Build the `Room` record: `{ id, blobId, width: normalized.width, height: normalized.height, createdAt: Date.now() }`. Use epoch `number` per CONTEXT JSON round-trip safety.
    6. Mutate the store via `useAppStore.getState().setActiveRoom(room)` (Plan 03 action).
    7. If `priorRoom && priorRoom.id !== room.id`: await `deleteBlob(priorRoom.blobId)` AND remove the prior room record via `useAppStore.setState((s) => { const { [priorRoom.id]: _evicted, ...rest } = s.rooms; return { rooms: rest }; })`. CONTEXT D-06 allows keeping the old room around because the schema is multi-room, but Phase 1 UI is single-room, so we evict eagerly to avoid quota growth. Phase 2 + v2 EDT2-01 multi-room work will revisit this policy.
    8. Do NOT touch `useAppStore.libraryItems` anywhere in this function — that absence is what satisfies UPL-03 by construction.

    Export `function useRoomUpload(): { uploadRoom: (file: File) => Promise<void> }` returning `{ uploadRoom }`. The hook is stateless in Phase 1; Phase 2 may add a `useState<'idle' | 'processing' | 'error'>` machine for the library upload UI.

    Do NOT add an `<input type=file>` here — RoomDropzone owns the empty-state input, and App.tsx (Task 3) owns the change-room input. Do NOT add a global error boundary; per RESEARCH §"Security Domain" all decode failures collapse to the generic toast.

    No dedicated unit test (D-13 cap). Verification is via grep + the manual smoke in Task 4.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm build && grep -q "HeicNotSupportedError" src/features/room/useRoomUpload.ts && grep -q "pipeline" src/features/room/useRoomUpload.ts && grep -q "setBlob" src/features/room/useRoomUpload.ts && grep -q "roomBlobId" src/features/room/useRoomUpload.ts && grep -q "setActiveRoom" src/features/room/useRoomUpload.ts && grep -q "deleteBlob" src/features/room/useRoomUpload.ts && grep -q "showToast" src/features/room/useRoomUpload.ts && grep -q "That photo format isn" src/features/room/useRoomUpload.ts && grep -q "Couldn" src/features/room/useRoomUpload.ts && ! grep -q "libraryItems" src/features/room/useRoomUpload.ts</automated>
  </verify>
  <done>
    `useRoomUpload` orchestrates pipeline → setBlob → setActiveRoom → cleanup-prior-blob in order; both error paths emit the locked UI-SPEC toast copy; libraryItems is provably untouched (grep negation); old room's blob is deleted when replaced.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build RoomCanvas (skeleton-first hydration with 150ms cross-fade + D-09 self-healing on IDB miss)</name>
  <files>src/features/room/RoomCanvas.tsx</files>
  <read_first>
    - src/features/room/SkeletonRoom.tsx (Plan 02 — what RoomCanvas renders during hydration)
    - src/store/useAppStore.ts (Plan 03 — activeRoomId + rooms selectors)
    - src/lib/idb/index.ts (Plan 01 — getBlob)
    - src/store/toastStore.ts (Plan 02 — showToast)
    - .planning/phases/01-foundations-image-pipeline/01-CONTEXT.md (D-08 hydration sequence; D-09 self-healing copy "We couldn't reload your last photo. Upload again to continue.")
    - .planning/phases/01-foundations-image-pipeline/01-RESEARCH.md §"Code Examples → Boot: skeleton-first hydration", §"Common Pitfalls" Pitfall 4 (React 19 StrictMode double-invocation guard)
    - .planning/phases/01-foundations-image-pipeline/01-UI-SPEC.md §"Interaction Contract" (150ms cross-fade on hydration AND on replace)
  </read_first>
  <behavior>
    - Test 1: RoomCanvas returns null when activeRoomId is null (App.tsx renders RoomDropzone instead)
    - Test 2: When activeRoomId is set, RoomCanvas immediately renders SkeletonRoom (synchronous; no waiting on IDB)
    - Test 3: A useEffect keyed on room.blobId reads the blob via getBlob; on success creates a URL via URL.createObjectURL; on cleanup calls URL.revokeObjectURL (cleanup runs on unmount AND when room.blobId changes — covers the Change-room replace flow)
    - Test 4: While url is null, only SkeletonRoom shows. When url is set, the img mounts with opacity 0 and transitions to opacity 1 over 150ms.
    - Test 5: When getBlob returns undefined (D-09 self-healing): emit showToast with title "We couldn't reload your last photo" + body "Upload again to continue." + variant 'error'; AND mutate useAppStore to set activeRoomId to null and remove the stale room record from the rooms map
    - Test 6: React 19 StrictMode dev double-invocation does NOT leak object URLs — useEffect cleanup runs before re-running; guard against setUrl after unmount via a `revoked` local flag (RESEARCH Code Example)
    - Test 7: img alt attribute is empty string (decorative user content)
  </behavior>
  <action>
    Write `src/features/room/RoomCanvas.tsx` mirroring RESEARCH §"Code Examples → Boot: skeleton-first hydration" closely.

    Imports: `useEffect`, `useState` from React; `getBlob` from `'../../lib/idb'`; `useAppStore` from `'../../store/useAppStore'`; `SkeletonRoom` from `'./SkeletonRoom'`; `showToast` from `'../../store/toastStore'`.

    Subscribe to the store with two narrow selectors (to avoid re-render storms): `activeRoomId` and the derived `room = s.activeRoomId ? s.rooms[s.activeRoomId] : null`.

    Local state: `url: string | null` (the object URL) and `loaded: boolean` (controls the opacity transition).

    Primary useEffect keyed on `room?.blobId`: if room is null, reset url + loaded to initial and return early. Otherwise declare a `revoked = false` flag and a `createdUrl: string | null = null` local, then fire an async IIFE that calls `getBlob(room.blobId)`. If revoked after the await, return. If blob is undefined, this is the D-09 self-healing case: call `showToast` with the locked self-healing copy (title "We couldn't reload your last photo", body "Upload again to continue.", variant 'error') AND atomically mutate the store via `useAppStore.setState((s) => { if (!s.activeRoomId) return s; const { [s.activeRoomId]: _evicted, ...rest } = s.rooms; return { activeRoomId: null, rooms: rest }; })` to evict the stale record. If blob exists, create the object URL via `URL.createObjectURL(blob)`, assign to `createdUrl`, and `setUrl(createdUrl)`. Wrap the entire IIFE in try/catch — on catch emit the generic upload-error toast. Return a cleanup function that flips `revoked = true`, calls `URL.revokeObjectURL(createdUrl)` if it exists, and resets `loaded` to false.

    Secondary useEffect keyed on `url`: when url becomes non-null, schedule `setLoaded(true)` on the next animation frame so the initial opacity-0 render commits before the transition fires. Use `requestAnimationFrame` + a `cancelAnimationFrame` cleanup.

    Render: if `activeRoomId` is null, return null. Otherwise render a wrapper `<div className="relative w-full max-w-3xl mx-auto rounded-2xl overflow-hidden shadow-lg">` containing `{!loaded && <SkeletonRoom />}` AND `{url && <img src={url} alt="" className="w-full h-auto block" style={{ opacity: loaded ? 1 : 0, transition: 'opacity 150ms ease-out' }} onLoad={() => setLoaded(true)} />}`. The `onLoad` callback is a fallback in case rAF timing loses to image decode.

    Do NOT add Konva — Phase 2 replaces this `<img>` with a Konva Stage. Do NOT add Transformer, drag handlers, or a library overlay. Plan 04 RoomCanvas is intentionally minimal — just an img on a rounded card per UI-SPEC §"Component Inventory".
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm build && grep -q "SkeletonRoom" src/features/room/RoomCanvas.tsx && grep -q "getBlob" src/features/room/RoomCanvas.tsx && grep -q "createObjectURL" src/features/room/RoomCanvas.tsx && grep -q "revokeObjectURL" src/features/room/RoomCanvas.tsx && grep -q "activeRoomId: null" src/features/room/RoomCanvas.tsx && grep -q "We couldn" src/features/room/RoomCanvas.tsx && grep -q "transition" src/features/room/RoomCanvas.tsx && grep -q "150" src/features/room/RoomCanvas.tsx</automated>
  </verify>
  <done>
    RoomCanvas paints SkeletonRoom synchronously when activeRoomId is set, asynchronously loads the IDB blob, creates + revokes object URLs correctly, fades in over 150ms, and self-heals with the locked D-09 toast + state cleanup on a missing blob.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire App.tsx (RoomDropzone vs RoomCanvas branch + ToastHost + Header onChangeRoom + boot navigator.storage.persist) and remove Plan 01 skeleton-ping demo</name>
  <files>src/app/App.tsx, src/app/AppShell.tsx, src/main.tsx</files>
  <read_first>
    - src/app/App.tsx (Plan 02 baseline — AppShell + Header(hasActiveRoom=false) + Footer + placeholder main with skeleton-ping demo)
    - src/app/AppShell.tsx (Plan 02 baseline — wraps children in .app-shell)
    - src/main.tsx (Plan 01 baseline — ReactDOM.createRoot + StrictMode)
    - src/lib/idb/index.ts (Plan 01 — skeletonPing export remains for future use; App.tsx stops calling it)
    - src/features/room/RoomDropzone.tsx (Plan 02), src/features/room/RoomCanvas.tsx (Task 2 of this plan)
    - src/features/room/useRoomUpload.ts (Task 1 of this plan)
    - src/components/ToastHost.tsx (Plan 02)
    - src/store/useAppStore.ts (Plan 03 — activeRoomId selector)
    - .planning/phases/01-foundations-image-pipeline/01-RESEARCH.md §"Open Questions" #3 + §"Common Pitfalls" Pitfall 5 (navigator.storage.persist at boot)
    - .planning/phases/01-foundations-image-pipeline/01-UI-SPEC.md §"Interaction Contract" (Change-room silently replaces; destructive confirm = Phase 3)
  </read_first>
  <behavior>
    - Test 1: App.tsx no longer imports `skeletonPing`; the Plan 01 debug paragraph is removed
    - Test 2: App.tsx subscribes to activeRoomId; renders RoomDropzone when null, RoomCanvas when set
    - Test 3: Header receives `hasActiveRoom={activeRoomId !== null}` and `onChangeRoom={openPicker}`; openPicker triggers `.click()` on a hidden `<input type="file" accept="image/*" capture="environment">` owned by App.tsx (so the Change-room flow works while RoomDropzone is unmounted)
    - Test 4: ToastHost is mounted inside AppShell so toasts render in the safe-area-aware tree
    - Test 5: A useEffect with empty deps calls `navigator.storage?.persist?.()` exactly once on boot; failures are swallowed
    - Test 6: When no room is active, Header's Change-room button is hidden (Plan 02 gate)
    - Test 7: pnpm build + pnpm preview serve a working SPA that round-trips reload (asserted manually in Task 4)
  </behavior>
  <action>
    Rewrite `src/app/App.tsx` to remove the Plan 01 skeleton-ping demo and wire the full Phase 1 flow.

    Imports: `useEffect`, `useRef` from React; `AppShell` from `'./AppShell'`; `Header` from `'./Header'`; `Footer` from `'../components/Footer'`; `ToastHost` from `'../components/ToastHost'`; `RoomDropzone` from `'../features/room/RoomDropzone'`; `RoomCanvas` from `'../features/room/RoomCanvas'`; `useRoomUpload` from `'../features/room/useRoomUpload'`; `useAppStore` from `'../store/useAppStore'`.

    Inside `function App()`:
    1. Subscribe to `activeRoomId` via `useAppStore((s) => s.activeRoomId)`.
    2. Destructure `{ uploadRoom }` from `useRoomUpload()`.
    3. Create `const inputRef = useRef<HTMLInputElement>(null);` for the Change-room hidden input.
    4. Add a boot-time `useEffect(() => { navigator.storage?.persist?.().catch(() => {}); }, [])` — fire-and-forget; per RESEARCH Pitfall 5 + Open Q #3 this dampens iOS 7-day ITP eviction. Swallow rejections (no UI surface).
    5. Define `const openPicker = () => inputRef.current?.click();` to trigger the hidden Change-room input.
    6. Return an `<AppShell>` containing in order: `<Header hasActiveRoom={activeRoomId !== null} onChangeRoom={openPicker} />`, then `<main className="flex-1 flex items-center justify-center px-4 py-6 md:py-12">` whose children are `activeRoomId === null ? <RoomDropzone onFile={uploadRoom} /> : <RoomCanvas />`, then `<Footer />`, then a hidden `<input ref={inputRef} type="file" accept="image/*" capture="environment" style={{ display: 'none' }} onChange={(e) => { const file = e.target.files?.[0]; if (file) uploadRoom(file); e.currentTarget.value = ''; }} />` (the value reset on every change is required so picking the same file twice still fires onChange), then `<ToastHost />`.

    Per CONTEXT/UI-SPEC, Change-room silently replaces in Phase 1 — no confirm dialog (that's Phase 3). The `uploadRoom` hook (Task 1) handles the entire replace flow including deleting the prior blob.

    `src/app/AppShell.tsx`: no functional changes required if Plan 02 shipped it with `flex flex-col` so `<main className="flex-1">` fills the remaining height. If Plan 02's version did NOT use a flex column, update it to `<div className="app-shell flex flex-col">{children}</div>`. Do not change padding (still owned by the `.app-shell` CSS class from Plan 02 Task 1).

    `src/main.tsx`: Plan 01 already shipped `<React.StrictMode><App /></React.StrictMode>`. Only touch if Plan 02 left it broken. Do NOT add a root error boundary — out of scope for Phase 1.

    Per CONTEXT deferred list, do NOT add a destructive-confirm dialog, BroadcastChannel multi-tab guard, onboarding tooltip, sample-room button, library upload UI, Konva stage, or PWA service worker. All deferred to Phases 2/3/5/6.

    The `skeletonPing` export remains in `src/lib/idb/index.ts` (Plan 01) — App.tsx simply stops calling it. The export is harmless and may serve as a future-phase smoke endpoint.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm vitest run && pnpm build && grep -q "navigator.storage" src/app/App.tsx && grep -q "ToastHost" src/app/App.tsx && grep -q "useRoomUpload" src/app/App.tsx && grep -q "RoomCanvas" src/app/App.tsx && grep -q "RoomDropzone" src/app/App.tsx && ! grep -q "skeletonPing" src/app/App.tsx && ! grep -q "Last skeleton ping" src/app/App.tsx</automated>
  </verify>
  <done>
    App.tsx wires the full Phase 1 flow; navigator.storage.persist is called once on boot; RoomDropzone / RoomCanvas branch on activeRoomId; Change-room button works via App-owned hidden input; ToastHost mounted; skeleton-ping demo removed; typecheck + lint + vitest + build all green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: End-to-end Phase 1 manual acceptance test (all 5 ROADMAP success criteria + D-09 self-healing)</name>
  <what-built>
    Plans 01–04 together deliver Phase 1: a deploy-ready Vite + React + TS + Tailwind v4 + pnpm SPA with AGPL-3.0 LICENSE + "Open source" footer link (FND-01, FND-02); a mobile-first safe-area-aware shell rendering correctly on iPhone SE (FND-03); a single ImagePipeline that EXIF-orients, rejects HEIC by magic-byte sniff, and resizes the long edge to ≤2048 px (FND-04); a camera-or-picker upload flow (UPL-01); a Change-room flow that silently replaces without losing the (empty) library (UPL-03); Zustand `persist` middleware with metadata-only `partialize` + `version: 1` + multi-room schema from day one (PER-03, PER-04); skeleton-first hydration with 150 ms cross-fade so reload re-renders the room photo at first paint (PER-02); and a D-09 self-healing flow when IndexedDB loses the blob. `navigator.storage.persist()` is called at boot to dampen iOS 7-day eviction.
  </what-built>
  <how-to-verify>
    1. Run `pnpm install && pnpm dev` — boots cleanly at `http://localhost:5173/`.

    2. **FND-03 mobile shell:** DevTools → device emulation → iPhone SE (375×667). Confirm: no horizontal scroll; safe-area padding visible; header tagline hidden at this width; Footer visible at the bottom with the AGPL link. Repeat on real iPhone if available — confirm safe-area insets around the notch + home indicator.

    3. **UPL-01 upload (desktop):** On the empty state, click the **Choose photo** button. The native file picker opens. Select a normal JPEG (5–10 MB OK). The photo appears in a rounded card with a brief cross-fade. Footer + Header remain visible.

    4. **UPL-01 upload (mobile):** Open the dev URL on a real iPhone. Tap **Choose photo** — confirm the iOS sheet offers "Take Photo or Video" / "Photo Library" / "Choose File" (the `capture=environment` hint).

    5. **FND-04 EXIF orientation:** Upload a portrait phone photo (one with EXIF orientation 6 — most iPhone portraits). Confirm it renders upright (not sideways or upside-down). Inspect DevTools → Application → IndexedDB → `keyval-store` → `keyval` → find the `room:<uuid>` Blob; its dimensions should reflect the upright orientation.

    6. **FND-04 ≤2048 cap:** Upload a photo whose original long edge is well over 2048 (4K phone photo). Inspect the stored Blob: width and height should both be ≤2048; the larger of the two should be exactly 2048.

    7. **FND-04 HEIC reject:** Upload an iPhone-native `.heic` file via desktop file picker. Expect the toast: title `That photo format isn't supported yet`, body `iPhone HEIC photos can't be opened in the browser. In Photos, tap Share → "Save to Files" as JPEG, then upload that.` No photo is added to the stage.

    8. **PER-02 reload survives:** With a photo loaded, hard-reload the page (Cmd-R or pull-to-refresh). Observe: the skeleton paints first (briefly), then the photo cross-fades in. No white flash, no flicker of empty state.

    9. **UPL-03 replace without losing library:** Click **Change room photo** in the header. The picker opens. Select a different photo. The new one cross-fades over the old. Inspect IndexedDB — the old `room:<uuid>` entry is GONE (cleanup), the new one is present. Inspect Zustand state via DevTools (Redux DevTools or window console: `JSON.parse(localStorage.getItem('roomdrop'))`) — `libraryItems` is `{}` (unchanged; v1 has no library yet, but the structural promise is that future library items would not be cleared).

    10. **FND-02 AGPL:** Confirm root `LICENSE` file exists and begins with `GNU AFFERO GENERAL PUBLIC LICENSE`. Confirm Footer reads `RoomDrop is **Open source** under AGPL-3.0` and clicking it opens `REPO_URL` in a new tab.

    11. **PER-03 partialize:** Open DevTools → Application → Local Storage → key `roomdrop`. Confirm the JSON contains only `{ schemaVersion: 1, rooms: { ... }, activeRoomId: '...', libraryItems: {}, placements: {} }` — NO blobs, NO base64, NO data URLs, no action functions.

    12. **PER-04 multi-room schema:** Inspect that `rooms` is a `Record<string, Room>` (an object keyed by UUID), not an array. `placements` is `Record<string, Placement[]>`. `libraryItems` is `Record<string, LibraryItem>`. Schema is in the v1 shape per CONTEXT D-06.

    13. **D-09 self-healing:** With a photo loaded, open DevTools → Application → IndexedDB → delete the `room:<uuid>` entry manually. Hard-reload. Expect: the toast `We couldn't reload your last photo` with body `Upload again to continue.` and the app returns to the empty-state dropzone. The stale `activeRoomId` is cleared from localStorage.

    14. **Generic decode failure:** Upload a file that is clearly not an image (e.g. a `.txt` renamed to `.jpg`). Expect the generic toast: title `Couldn't open that photo`, body `The file may be corrupted or too large. Try a different photo.`

    15. **Build portability:** Stop `pnpm dev`. Run `pnpm build && pnpm preview`. Repeat steps 3, 5, 8, 9, 13 against the production bundle on the preview URL. Behavior should be identical. Run `grep -q "./assets/" dist/index.html && echo PORTABLE` — must print `PORTABLE`.

    16. **No PWA / no Husky / no Playwright:** `cat package.json` — confirm `vite-plugin-pwa`, `husky`, `@playwright/test` are NOT present. (Phases 6 and 4 will add PWA + Playwright respectively.)
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 1 complete, or describe failing acceptance criteria (cite the step number above).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User file → useRoomUpload → ImagePipeline | Untrusted bytes enter; HEIC sniff + decode failure both collapse to toasts |
| RoomCanvas → IndexedDB (getBlob) | Local-origin trust; missing blob triggers self-healing flow |
| App.tsx → navigator.storage.persist() | Permission-API surface; failure is swallowed (no UI surface) |
| useRoomUpload → useAppStore.setState | Direct store mutation; constrained to `setActiveRoom` action + a single inline setState for room eviction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Denial of Service | Concurrent uploads racing (user double-taps Choose photo) | accept | Phase 1 has no loading state UI per UI-SPEC, but the upload flow is fully async + idempotent — each upload generates a fresh UUID, prior blob is deleted only after new blob is written. Worst case: two pipelines run; the second to call `setActiveRoom` wins; the first's blob may leak in IDB until next replace. Phase 2 adds `useState<'idle'|'processing'>` to gate the button. |
| T-01-04-02 | Information Disclosure | navigator.storage.persist() permission state leak | accept | Per W3C Storage spec, persistence grant is per-origin and visible only to same-origin scripts; no cross-origin leak. Best-effort call with swallowed rejection. |
| T-01-04-03 | Tampering | localStorage hand-edited to point at non-existent IDB blob | mitigate | D-09 self-healing flow in RoomCanvas (Task 2) catches this — emits the locked toast and evicts the stale record. Tested manually in Task 4 step 13. |
| T-01-04-04 | Information Disclosure | EXIF GPS / device metadata in uploaded file | mitigate | Pipeline (Plan 03) re-encodes via `canvas.convertToBlob({ type: 'image/jpeg' })` which strips EXIF entirely. Only normalized blob is persisted. |
| T-01-04-05 | DoS | Object-URL leak (cross-fade transitions accumulating URLs) | mitigate | Every `URL.createObjectURL` is paired with `URL.revokeObjectURL` in the same useEffect's cleanup; the React 19 StrictMode `revoked` flag prevents post-unmount setUrl + leak. RESEARCH Pitfall 4 explicitly addressed. |
| T-01-04-06 | Tampering | Blob replaced in IDB by an attacker with write access to user's device | accept | Attacker with device write access has full origin compromise — no client-side mitigation possible. CLAUDE.md hard constraint says no accounts/cloud sync, so this remains the user's local-device threat boundary. |
| T-01-04-07 | XSS | Object URL injected into `<img src>` | mitigate | `URL.createObjectURL` only produces `blob:` URLs (per spec); cannot produce `javascript:` URLs. React's JSX `src` attribute does no URL parsing beyond the value. Confirmed no `dangerouslySetInnerHTML` anywhere in this plan. |
</threat_model>

<verification>
- `pnpm install && pnpm typecheck && pnpm lint && pnpm vitest run && pnpm build` all exit 0.
- `pnpm preview` serves a working SPA round-tripping reload.
- All 16 manual acceptance steps in Task 4 pass.
- AGPL LICENSE + footer link visible; partialize payload contains no blobs; multi-room schema shape verified; D-09 self-healing verified by manual IDB deletion.
- No `vite-plugin-pwa`, `husky`, `@playwright/test` in dependency tree.
</verification>

<success_criteria>
- All 5 ROADMAP Phase 1 success criteria pass (mobile shell, camera/picker upload + HEIC reject, EXIF orient + resize on replace, reload-survives-at-first-paint, AGPL LICENSE + footer link).
- D-09 self-healing flow proven manually.
- libraryItems is preserved across room replace (UPL-03 structural promise).
- Phase 1 is deploy-ready, not deployed (D-14) — `dist/` is portable static bundle.
</success_criteria>

<output>
Create `.planning/phases/01-foundations-image-pipeline/01-04-SUMMARY.md` documenting:
- Whether all 16 manual acceptance steps passed; for any failure, the step number and the gap.
- Whether the iPhone SE smoke was performed on real hardware or only DevTools.
- Whether HEIC reject was tested with a real iPhone HEIC file (recommended) or only with the magic-byte fixture.
- Whether `navigator.storage.persist()` returned true or false on the test device.
- Confirmation that no files in this plan overlap with Plans 02/03 (only `src/app/App.tsx`, `src/app/AppShell.tsx`, `src/main.tsx`, `src/features/room/RoomCanvas.tsx`, `src/features/room/useRoomUpload.ts` were modified; Plan 02 only created App.tsx and AppShell.tsx, so this plan extends — not conflicts with — those files since Plan 02 ships first).
- Append the new D-09 self-healing toast copy to UI-SPEC's toast catalog (one-line edit).
- Final committed `REPO_URL` value (or note that the placeholder is still in source).
</output>
