# Plan 03-01: Export & Reset (single plan)

**Phase:** 3 — Export & Reset Semantics
**Status:** planned
**Requirements:** EXP-01, EXP-02, EXP-03, PER-05

## Tasks
1. **Export module** — `composeRoomPng(room, placements, items)` draws room + placements at native resolution on an offscreen canvas; `deliverPng(blob, filename)` shares (mobile) or downloads (desktop); unit tests for compose math with canvas mocks + filename format.
2. **ConfirmDialog** — accessible modal (role=dialog, Escape, backdrop click cancels; danger-styled confirm option).
3. **Store/reset plumbing** — `clearPlacements(roomId)` action; `resetEverything()` feature helper (store reset + IDB wipe); uploadRoom drops evicted room's placements.
4. **Header actions** — Export button (busy state) + overflow menu wiring the three confirmed actions.

## Verification
- `pnpm typecheck && pnpm lint && pnpm test && pnpm build` green.
- Phase 3 success criteria traced in SUMMARY.
