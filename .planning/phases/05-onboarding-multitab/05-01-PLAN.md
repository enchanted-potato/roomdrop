# Plan 05-01: Onboarding & Multi-Tab Safety (single plan)

**Phase:** 5 — Onboarding & Multi-Tab Safety
**Status:** planned
**Requirements:** FND-05, ONB-01, ONB-02, ONB-03, PER-06

## Tasks
1. **noticesStore** — localStorage-backed one-time flags (honesty, coachmark) with useSyncExternalStore.
2. **Sample assets** — `scripts/generate-samples.mjs` (sharp) → `src/assets/samples/` (room.jpg + 4 alpha PNGs); commit generated files.
3. **Onboarding UI** — HonestyNotice card, SampleRoomButton + `loadSampleRoom` (idempotent fixed-id items, pre-cutout), Coachmark after first placement.
4. **Multi-tab** — `useSecondTabWarning` (BroadcastChannel hello/present) + SecondTabBanner.
5. **Tests** — noticesStore flags; loadSampleRoom idempotence (mock fetch of asset URLs).

## Verification
- `pnpm typecheck && pnpm lint && pnpm test && pnpm build` green.
